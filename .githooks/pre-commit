#!/bin/sh
set -eu

repo_root=$(git rev-parse --show-toplevel)
if command -v python >/dev/null 2>&1; then
  python_cmd=python
else
  python_cmd=python3
fi

exec "$python_cmd" "$repo_root/tools/release_sync.py" pre-commit
