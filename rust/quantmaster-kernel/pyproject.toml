[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "quantmaster-kernel"
dynamic = ["version"]
requires-python = ">=3.10"

[tool.maturin]
module-name = "_quantmaster_kernel"
