# 审查问题推进记录（2026-09-19）

本记录追踪 2026-09-14 项目审查的后续处理，区分源码修复、验证、合并和本机运行验收。旧报告描述的是当时的固定基线，不能直接作为当前缺陷清单。

## 本轮范围

优先处理成交语义、历史研究证据、已证实的冗余代码与迁移维护债务。另行修复运行检查中发现的任务耗时和 StockDB 健康探针问题。“今日工作台”各页面由独立任务负责，最后再核对整合结果。

## 问题与当前证据

| 项目 | 原问题或本轮发现 | 处理与验收状态 |
| --- | --- | --- |
| F1 成交资格 | 缺失停牌或复权证据仍可成交 | 已由 PR #494 修复，本轮不重复改写 |
| F2 执行资金 | 旧仓卖不出时新增买入可能产生未融资仓位 | 执行器已由 PR #497 修复；持久化旧证据的消费限制见下项 |
| F3 买入数量 | 最低佣金边界下，回测拒绝整单而模拟交易可以成交 | PR #572 已合并；共享费用约束计算；159 项本地测试、完整 CI 通过，任务清理完成 |
| 历史执行证据 | 旧报告仍可能作为审批、部署或策略评分依据 | PR #578 待集成；审批、指定周期部署、实时及历史评分均检查当前执行契约；原始报告保留 |
| D1–D3 冗余 | Help CSS 重复、无调用函数、没有实现的 Backtrader 安装入口 | Issue #569 执行删除及界面验收中 |
| D4 迁移债务 | 测试绑定旧模块结构，迁移缺少明确退役条件 | PR #576 待集成；建立 14 个持久化契约的退役依据，删除固定源码结构测试；不盲目删除仍需读取历史数据的转换器 |
| 任务耗时 | 已完成的 7 秒任务，隔日可能显示十几万秒 | PR #579 待集成；终态任务按结束时间计算耗时 |
| StockDB 健康探针 | SDK 返回空近期行情仍可报告正常 | PR #580 待集成；验证有界近期窗口的有效记录，明确不代表完整市场新鲜度 |
| StockDB 最新数据 | 最新分区缺文件，更新多次失败，已有旧分区仍可读取 | Issue #517 仍未解决；健康状态修复不等于数据恢复 |

## 历史研究结果：保留事实，阻止错误复用

本轮对实际使用的本机研究数据库作只读检查：222 个因子版本、70 份验证报告，其中 28 份通过；8 个版本处于 approved 状态。70 份报告均未携带当前 `self_financing_open_v1` 执行契约。数据库没有部署记录、部署证据或策略候选记录，因此不能把旧审批问题描述成已发现正在运行的错误 Champion。

修复不修改旧报告内容或哈希，也不把旧结果补写成符合新契约。旧证据只作为历史记录，重新审批、部署和参与策略计算前必须复验。Python 复验路径同时改用原始开盘价执行，避免以复权价格对比原始涨跌停价格。

本机复验预检显示，现有本地 CSI800 数据可以用于沙盒研究，但 800 个标的尚缺完整单位、复权、提供商、交易日历或新鲜度证据，不能据此宣称生产级可部署。重新计算成功与满足部署条件是两项不同验收。

## 数据恢复：已确认的边界

本轮检查时，StockDB 目标交易日为 2026-09-18，状态仍停留在验证到 09-17，最新目标覆盖为 0/5543。09-18 日志报告当前分区缺少 29 个文件，待恢复数据约 883 MB；检查到的备份与升级目录没有可直接恢复的数据分区。旧日期的部分行情仍可读取，不能整体禁用 StockDB。

现有 Tushare 缓存只能覆盖部分日期与标的。恢复继续使用已经配置的数据源，先检查本地缓存，再验证缺失日期是否可以通过原提供商的批量查询补齐。不会将空响应、零值填充、随机公共数据或未经证实的复权数据作为恢复成功。

“Rust 尚未使用 NumPy 零复制”和“当前仍缺 45 个 StockDB 文件”均不应继续列作本轮结论：前者已实现，后者是旧时间点的观察。本记录使用本次实际检查结果。

## 尚待完成的验收

- 记录各 PR 的最终提交、完整 CI、集成及任务清理结果。
- 本机安装本轮合并后的版本，检查旧证据使用限制、终态耗时和 StockDB 探针行为。
- 保留历史报告校验依据，记录是否进行了复验及其真实结果。
- 等待独立任务完成“今日工作台”，再检查页面整合结果。

本记录只对有证据支持的项目作完成判断，不把合并、状态提示改善或测试通过当作真实数据恢复。
