# Changelog

## v1.16.2（2026-08-16）

### A 股恐贪指数
- 接入 FundDB A 股恐贪指数，支持上证指数/沪深300 口径切换，与 CNN 全球恐贪指数并行展示；页面新增独立 A 股面板、指标仪表盘和历史曲线
- 适配器复用 FundDB 公开网页客户端的图表请求并校验 date/fear/index 三列契约，响应加密变化时显式降级，不将未知响应静默解释为新数据
- 两个口径分别以显式 symbol 刷新，日缓存优先、失败安全降级到最近成功值；RSI 机会逻辑和 CNN 指数保持原口径，不合并也不替换

## v1.16.1（2026-08-16）

### 启动时间预算门禁与零拷贝 Rust 内核
- onedir 安装实例的 CLI --help 冷启动 ≤ 1.5s、Web core_ready ≤ 5s 硬门禁生效，采样为 1 次冷启动 + 2 次热启动中位数，测量证据写入 JSON
- Rust 研究内核改用 PyO3 numpy crate 零拷贝传递 NumPy 数组，消除 _to_native/_from_native 转换开销；全市场 2500 标的 × 760 日基准净加速约 18x
- 桌面发行包裁剪未使用的 Feishu SDK 家族（Sheets/Tasks/VC/Wiki/Workplace 等）和 PyArrow Flight/Substrait/CUDA 等可选后端；包体门禁同步更新

### 任务生命周期与本地卫生
- tasks.py remove/gc 在删除任务工件后自动清理对应的 .task-running.lock 标记；gc 额外清理孤儿锁标记，锁文件不再累积
- CI 报告与 tasks.py 日志脱敏，不再包含本地绝对路径；新增路径安全测试
- 复杂度 ratchet 审计支持 owner 授权的基线更新命令（--accept），机械增长不再阻塞整个质量门禁

## v1.16.0（2026-08-15）

### 产品、架构与协作基线
- 固定日常决策与量化研究双核心、四工作区、市场能力边界、正式/Sandbox 语义及包体与更新预算
- 删除重复的 `validate`、`grid`、`fund-test`、`mine` 和 `mine-llm` 顶层命令；正式研究统一从 Quant Lab 进入，保留 `factor-test` 与 `backtest` 薄入口
- `qm factor-test` 与 Web 因子体检共用 factors 能力内的点时证据与数值计算入口；CLI 仅保留参数解析和摘要输出
- `qm backtest` 与 Web 回测任务共用 backtest 能力内的 PIT、质量门禁、策略冻结、撮合和报告入口；新结果显式记录正式资格，Sandbox、Lab OOF 与旧未分类结果禁止晋升模拟账户
- 明确模块化单体依赖矩阵、唯一 composition root、不可变稳定槽与开发 worktree 隔离合同
- 后续任务统一通过 GitHub Issue、Draft PR、Actions 与 Discussion 管理；agent 负责合并和 tag 操作，正式 Release 仍需 owner 明确确认
- Windows 打包进程在导入应用前将配置、数据与 StockDB 默认根目录绑定到用户 AppData；YAML 与绝对启动覆盖继续优先，缺失或相对路径会在未写盘时明确失败

### 数据可靠性
- 全球参考行情的缺口规划、来源降级、质量门禁与缓存写入收拢到统一数据入口，并修复刷新校验遗漏标的身份导致新数据被拒绝的问题
- 每日估值在本地基本面缓存未命中时优先复用盘后 StockDB 截面；未经验证的估值字段保持为空，并沿用现有远端回退
- 因子验证与模型研究共用可配置的三年训练、一年测试、年度前滚和 purge 协议；数据不足明确报告所需交易日，负向时移、居中滚动和前缀污染均硬失败
- Lab 本地面板加载公开检查、行情读取、拼装、成员与证据冻结分段耗时，并跨进程复用已校验的冻结矩阵，避免重复读取数百个标的后再次写入同一证据
- 因子目录补充含义、逻辑、标签、周期、验证状态和版本历史筛选；2–30 个同周期表达式因子可在同一冻结快照上生成日截面 Spearman 热图，并按设置中的 |ρ| 阈值标记高相关因子对
- 每个因子版本与预测周期可打开独立鲁棒性详情，使用冻结的紧凑分布、参数邻域、WFA 窗口及年份/状态/流动性/个股穿透证据展示图表、阈值、结论与中文解释；旧证据缺少绘图契约时明确提示重新验证

### 开发流程与 CI 分层
- Draft PR 只运行 Ruff、异常/复杂度策略、mypy 与核心契约快检；标记 Ready 后触发覆盖分片、native parity、browser 与 Windows 打包完整矩阵
- `tasks.py ready --accept-ci` 复用同一 commit 上含完整重 job 的绿色 CI 证据，集成阶段不再本地重复全套验证
- GitHub Issue/PR 状态由 `scripts/dev/github_sync.py reconcile` 自动对账，重复或已合并但仍 open 的记录可安全修复；版本变更集中在单独版本 PR
- CodeQL 只扫描 main 与定时计划，Draft PR 不再重复运行；Dependabot 使用合法 pip 配置按周更新

## v1.15.4（2026-08-14）

### 时间语义与点时安全
- 业务日期 `as_of_date` 与精确信息截止 `cutoff_at` 分离，多市场时钟使用 IANA 时区，并显式区分盘中部分日线、等待 provider、等待摄取和完整日线
- StockDB 部分覆盖不再唤醒盘后正式流水线或模拟交易；历史新闻默认按系统首次观测与分析完成时点回放，迟到新闻和迟到行情不得穿越进入过去决策或成交
- 财务公告日期不再被解释为当日零点；日期级公告最早从下一真实交易 session 可见，精确发布时间则严格按带时区的信息截止时刻判断
- 资讯页面分别展示发布、首次观测、最近抓取和内容版本时间；缺失或不可解释的发布时间以诊断码明确标记
- 后台状态按 CN/HK/US 展示市场时区、session 完成度、最近完整日线、Provider/摄取等待与时间诊断；无可信日历或时间证据时明确显示不可用
- 行情 registry 按 A/H/美股各自 IANA 时区和交易时段解释分钟数据；无 provider 时区或无产品级期货时段证据时显式拒绝，盘中日线保持 partial

### 标的身份与 provider alias 治理
- 内部标的身份与展示代码、provider symbol 分离；alias 按 provider 和有效期管理，冲突元数据会被拒绝，历史请求按 as-of 选择当时映射
- 裸代码只生成搜索候选，跨市场或跨资产类型时必须明确选择；旧记录证据不足时保留原始代码与待确认状态
- `GC/CL/HG` 明确为不可交易的 provider 连续期货序列，记录交易所、乘数、报价单位与未公开的 roll/adjustment；`USD-CNY` 以 base=USD、quote=CNY 存储
- 搜索、详情与设置诊断补充交易所、资产类型、币种、歧义、provider 覆盖、历史 alias 缺口和诊断码，并优化窄屏与键盘/读屏交互

### Provider 与缓存真实性
- Tushare 响应在写缓存前校验声明字段、标的身份和日期范围；资讯响应拒绝空内容、媒体类型错配以及登录、验证码和上游错误拦截页
- Provider 失败映射为明确的限流、权限、无效响应或暂态失败，不再与有效空结果混淆；接口缓存身份纳入 provider 配置版本
- 历史缓存必须覆盖 `as_of` 前可信的最后交易 session；缺少可信日历或尾部数据时 formal research 明确不可用，港美市场不会被 A 股日历误认证
- 缺少数值语义的旧行情缓存不再与增量响应拼接，而是由单一 provider 完整替换；多个降级候选优先保留数据截止日最新者

### 存储、研究与界面可靠性
- control DB writer lease 按 PID、进程启动时间和数据库路径核验，诊断可区分真实写者、PID 复用和路径漂移
- Quant Lab 在 bars、dataset 和 snapshot 写入前估算原子替换峰值并保留安全空间；磁盘不足、SQLite full 或 I/O 错误会持久化阻断检查点并安全重试
- 收盘后工作台扩大板块优先区，在 1120px 以下切换为单列，避免板块表格横向滚动

### 开发与发布基线
- 独立任务固定开发基线，完成后只对齐一次最新 main 并验证，发布候选继续冻结为不可变完整 SHA
- 任务清理可恢复旧 Windows ACL 继承；仍被历史运行时占用的残余目录保留明确证据供后续生命周期重构处理

## v1.15.3（2026-08-13）

### 缓存治理与可观测性
- 缓存身份、命名空间、失效和来源证据使用明确契约；缺少 provenance 时显式失败，不再把不完整证据解释为当前数据
- 数据源新鲜度、部分成功和重试状态可持久恢复，设置诊断新增缓存命名空间、覆盖与失效状态，便于定位陈旧或不完整数据

### Quant Lab 模型契约
- Lab 模型迁移到当前唯一 schema，旧模型通过显式迁移或明确拒绝处理，不再保留重复的历史解码路径

### 开发工作流
- 本地验证按改动范围运行并复用同一提交的完整成功证据，自动清理成功运行的临时目录；运行时临时目录保持继承 ACL，降低重复 CPU、磁盘负载和 Windows 清理失败
- 已完成或明确被替代的任务可由任务工具按证据安全清理，成功验证产生的工作树制品也会自动回收

### 代码可维护性
- ETF 正式画像将不可变目录加载与降级、生命周期校验、份额时点筛选、完整 artifact 复验、增强元数据合并和画像构造拆为独立阶段，保持生产发布资格、时间边界和错误语义不变
- ETF sandbox 画像将证据截止、候选筛选、元数据与份额观察选择、生命周期校验和母集发布拆为独立步骤，保持探索层级、时间边界与禁止正式发布的契约不变
- 轮动快照构建拆分为输入复用、远端补充、矩阵装载、分维计算与原子发布阶段，保持快照字段、检查点、降级错误和持久化顺序不变

## v1.15.2（2026-08-13）

### 发布工作流
- 发布候选现在冻结为 origin/main 历史中的完整 commit SHA；人工确认期间 main 可继续前进，最终显式发布仍精确指向该候选。

## v1.15.1（2026-08-13）

### 设置与迁移安全性
- 设置检查和候选配置预览会保留用户已配置的候选值，避免诊断或预览流程用当前生效值覆盖尚待确认的修改
- 旧版契约迁移的验证备份移到实时数据目录之外，恢复与清理流程不会把备份误识别为待迁移的活动数据

## v1.15.0（2026-08-13）

### 持久化调度与运行时治理
- 自动化调度触发器、消息投递和分析卡片改为可恢复的持久化执行，进程重启后能够续接租约、进度和待投递结果，并在界面展示调度与运行时生命周期状态
- Web 流、后台任务、StockDB 与 worker 按运行代际协同关闭，避免热重载、退出和子进程派生期间残留旧任务；飞书监听支持安全切换凭据、确认就绪并正确清理事件循环
- 持久后台执行器启动时立即刷新 CNN 恐贪快照，成功后每 30 分钟更新，暂态失败则短间隔重试；Web 页面继续只读取本地快照

### 存储诊断与可恢复迁移
- 设置中心新增只读存储状态、实例路径、检查点和 SQLite 恢复诊断，可识别损坏、活跃 sidecar、路径漂移与备份可用性，并通过受保护流程执行恢复
- 资讯、自动化、行情、决策、盘后和纸面交易的旧版契约改为显式、可追踪的领域迁移；缺少证据或迁移失败时明确阻断，不再静默解释为当前格式
- 设置修订和运行时漂移纳入统一治理，配置路径与诊断绑定当前实例，降低多实例或工作树之间误用状态的风险

### 纸面交易与市场状态
- 纸面订单撮合、工作进程状态和恢复工具形成独立生命周期，界面区分订单与 worker 状态，并保留可审计的迁移和恢复记录
- 轮动行业分类、数据提供方与 ETF 研究状态使用明确契约，补充分页、迁移进度和降级证据，减少旧缓存与当前目录混用

### Windows 进程管理
- 源码启动器改为由 `QuantMaster.exe` 直接承载 CPython 运行时，不再经过隐藏的 `python.exe` 重定向层；Windows 可按 QuantMaster 根进程折叠 StockDB、Web 与运行时 worker
- Web、运行时与计算 worker 使用各自的镜像名，并修复命名 worker 再派生子进程时回退为普通 `python.exe` 的问题；发行版也会在导入应用前正确分派 multiprocessing 子进程

### 代码可维护性
- ETF 目录与份额同步拆分为会话解析、元数据增强、历史回填规划、逐日采集和证据化合并阶段，保持本地优先、降级错误、持久化与返回契约不变，并继续收紧 Ruff C901 库存
- 个股研究引擎将数据采集、联网检索、证据组装、技术评分、六维推理与最终审阅拆为独立阶段，保持超时、取消、检查点、降级路径与报告格式不变


## v1.14.5（2026-08-13）

### 数据恢复与错误契约
- free-stockdb 摄取会保留并校验可用的部分数据，在远端补齐失败时明确报告缺口，同时避免丢弃已经验证的本地证据
- Web API、后台任务与前端统一采用结构化错误契约，稳定暴露错误代码、摘要、可重试状态与恢复建议，减少不同入口之间的错误语义漂移

### 轮动分析可维护性
- 轮动分析将市场温度、板块信号、ETF 资金证据与申赎统计拆为可独立测试的决策步骤，保持现有结果字段、错误语义和历史窗口不变
## v1.14.4（2026-08-13）

### 运行时诊断与自动化可靠性
- Web 启动、模型服务、数据源与任务队列的失败诊断改为保留可操作的状态与恢复信息，减少端口冲突、上游不可达和队列阻塞的排查时间
- 飞书 Bot 连接增加 EOF、超时、TLS 与连接重置分类、脱敏错误记录和有界退避重连；证书验证错误仍明确停止，不降低 TLS 安全性
- 资讯摄取与数据源恢复会保存更细粒度的进度、重试和健康证据，页面可展示来源处理进度并避免把暂态失败误报为永久不可用

### 代码可维护性
- 回测引擎将行情准备、风险退出、调仓执行与收盘结算拆为明确阶段，保持交易与错误契约不变，并收紧 Ruff C901 历史复杂度库存
- 轮动持久化将快照规范化、索引行构造、分页查询与 ETF 元数据完整性校验拆为独立契约，保持 SQLite 格式、哈希和错误语义不变
- 轮动分析将市场温度、板块信号、ETF 资金证据与申赎统计拆为可独立测试的决策步骤，保持现有结果字段、错误语义和历史窗口不变

## v1.14.3（2026-08-13）

### 数据契约与运行时维护
- 已发布版本的日期校验改为按历史版本记录核对，防止跨日后错误要求旧版本发布日期等于当前日期
- 轮动持久化拆分为快照规范化、索引行构造、分页查询与 ETF 元数据完整性校验等独立契约，保持现有 SQLite 格式和错误语义
- 可信旧版行业映射会迁移为当前可追溯格式，保留历史分析所需的行业归属信息

## v1.14.2（2026-08-12）

### AI 与资讯任务稳定性
- 应用关闭或热重载时，仍在使用中的模型 HTTP 连接会等请求退出后再关闭，避免客户端池清理与并发请求退出互相争用
- 资讯抓取批次支持按不可变证据安全重放；仅完全一致的批次视为幂等成功，任何元数据或文章证据差异都会明确拒绝
- 补充资讯分析租约在 SQLite 短暂锁冲突后的恢复与线程退出验收，防止历史心跳异常回归

## v1.14.1（2026-08-12）

### 跨平台 CI
- Windows 控制台关闭处理器测试现在可在没有 `ctypes.WinDLL` 的 Linux Python 上显式注入模拟入口，修复完整覆盖率分片在进入被测逻辑前失败的问题

### 服务重载
- Web reload 改为仅在明确请求时启用，普通 `qm serve` 不再默认启动重载监督进程，减少多余的父子进程生命周期复杂度

### ETF 研究布局
- 扩大 ETF 研究结论区的可用宽度并收紧内部留白，改善高信息密度内容和窄屏缩放下的阅读与横向溢出表现

### 选股决策数据
- 正式选股会先复用本地 StockDB 与缓存，证据不足时在线补齐并写回；在线不可用但本地覆盖率不低于 90% 时仍可按现有证据保存，低于门槛或有效历史不足时明确阻断
- 旧版无可信哈希的历史快照改为显式降级预览，不再让已生成的新选股结果卡在 96%；严格证据读取仍拒绝把旧记录冒充可信快照

## v1.14.0（2026-08-12）

### 设置页面布局
- 在线数据源诊断改为顶部对齐且各自按内容决定高度，Tushare 检测结果不再被综合数据源的长详情拉伸或压到中部
- 自动化的重点观察列表与市场哨兵指数在宽屏并排显示并增加编辑高度，窄屏仍自动回落为单列

### 模型服务检测
- 模型目录与联网搜索检测通过受认证的 worker IPC 一次性交接临时凭据，修复跨进程执行时误报凭据失效；凭据不会进入任务、事件、产物或公开响应
- 刷新状态使用紧凑且尺寸一致的取消按钮，桌面状态栏与移动端触控布局同步整理

### 研究数据设置
- 将不明晰的“跨资产研究生产湖”整理为默认折叠的“版本化研究产物”高级工具，明确其产物、回测与 Quant Lab 消费场景，以及普通看盘和候选扫描无需操作的边界
- 研究产物目录改为展开后按需读取，并按“用途说明—可用能力—任务范围—估算确认—执行进度”组织，减少日常设置噪音和无意义的后台请求

### Quant Lab 云端样本
- “自动发送匿名云端样本”使用明确的两态合同：关闭时每次出站前确认，打开后直接发送；服务端同时校验逐次确认，避免界面绕过安全边界
- 项目虚拟环境补齐锁定版本的 Tushare 可选依赖后，csi800 候选检测不再误报依赖缺失

## v1.13.0（2026-08-12）

### 在线数据源控制
- 设置中心新增 free-stockdb-online、AKShare、Tushare 与 Yahoo Finance 独立开关；关闭后统一调度不再请求对应来源，诊断会明确显示为用户关闭
- free-stockdb-online 可按需配置服务地址与超时，只作为小批量交互补缺的末位来源，不进入普通后台或正式研究请求链

### 本地行情与刷新性能
- 观察、选股和盘后轮动优先复用本地 StockDB 的批量行情、证券资料与板块证据，仅在本地数据确实缺失、过期或无效时进入在线后备来源
- 扩大本地摄取批次并有界并行行情刷新，移除页面刷新主链路中的逐标的联网补证，显著减少数据库重复打开与外部请求

### Windows 托管进程生命周期
- 托管服务和 worker 统一标记 QuantMaster 进程角色并保持在应用进程树内，修复 reload 与子进程启动时脱离管理的问题
- 控制台收到 Ctrl+C 或 reload 退出时会关闭所拥有的 StockDB 与 worker，避免后台托管进程残留

### 项目维护
- AI 协作规范与设计上下文改为仅保留在本机，不再进入 GitHub 仓库；补充 worktree 复用项目虚拟环境、受限 Git 授权和隔离 uv 缓存的本地约束

## v1.12.2（2026-08-12）

### 观察与选股刷新性能
- 观察和选股的本地行情面板改为一次批量读取，证券资料与板块行情也复用批量查询，避免逐股票反复打开数据库和行情文件
- StockDB 全市场摄取扩大本地批次并复用本地校验通过的不可变数据；盘后扫描和轮动默认只读本地证据，不再把在线补证和目录同步塞进页面刷新主链路
- 行情刷新任务按最多八个标的有界并行，完整但仍待外部证明的 StockDB 行情不再逐源联网补证；盘后状态文案改为可读中文，隐藏难懂的字段缩写和内部哈希

### 在线数据源开关

- 设置中心新增 free-stockdb-online、AKShare、Tushare 与 Yahoo Finance 独立开关；关闭后统一行情调度不再请求对应来源，诊断会明确显示为用户关闭
- free-stockdb-online 可按需启用并配置服务地址与超时，只作为小批量交互补缺的末位来源，不进入普通后台或正式研究请求链

### 设置与数据源诊断
- 模型服务检测改用进程内一次性凭据引用，不再为临时诊断写入 Windows Credential Manager，消除凭据冲突并恢复模型目录与联网检测
- 数据源检测允许显式探测本机 provider，避免已运行的 free-stockdb 被本地访问保护误报为离线；AkShare 缺失提示同步给出 `uv sync --extra data` 安装命令

### 资讯时效与标注进度
- 新浪财经在历史缺口回补期间仍会优先探测首页，并利用可信时间下界结束无效回补，避免最新资讯长期停滞
- 手动标注不再复用已经结束的旧任务，进度按实际批次更新，部分失败会明确报告，操作按钮在任务真正结束前保持忙碌状态

### 隔离任务工作流
- 隔离 worktree 统一发现主工作区虚拟环境，并将 pytest、Ruff 与 mypy 缓存写到任务专属制品目录，避免验证污染任务树
- Windows worktree 清理可恢复 Git 已移除登记但实体目录仍存在的状态，安全处理虚拟环境目录联接，并仅在确认已集成且干净后删除任务分支

## v1.12.1（2026-08-12）

### 观察与选股本地数据优先
- 本地 StockDB 行情可用于分析时不再为了补充单位、复权证明而逐个请求 Tushare 或 AKShare；仅在本地数据确实缺失、过期或结构无效时进入后备来源
- 指数候选预览优先读取本地完整成分快照，并修复 AKShare 专用入口绕过页面本地只读边界的问题
- 行情质量、来源健康和板块快照提示改为面向用户的中文说明，不再直接展示内部通道名、Python 异常、字段缩写或算法版本号

### 任务工作流约束修正
- 明确只有合入 main 的 release 才递增版本，任务分支 checkpoint 不得修改发布元数据，消除新旧约束之间的歧义
- 补充 `ready` 对主分支、脏工作区、落后分支和发布文件改动的合同测试，确保集成前置条件可持续验证

## v1.12.0（2026-08-12）

### 并行任务与分层验证
- 每个 feature 或 bug 使用独立 worktree 和任务分支，开发提交不再争抢版本文件；完整验收后 squash 为一个 main release
- 新增显式影响测试映射、保守完整套件回退和历史耗时均衡分片，缩短开发反馈，同时保持发布前完整验证

### 桌面包体积治理
- PyInstaller 仅收集实际需要的 SciPy FFT 兼容模块，并显式排除 Torch、Dask、pytest 和包内测试，避免完整开发环境生成超大 EXE
- 本地 CI、GitHub CI 和正式发布新增 350 MiB 体积及禁用模块门禁，阻止异常桌面包进入发布流程

## v1.11.5（2026-08-12）

### 一键启动入口恢复
- 恢复仓库根目录 `qm-serve.cmd` 双击入口，并将参数和退出码透明转发到 `scripts/dev/serve.cmd`，继续使用源码、虚拟环境和本地数据启动

## v1.11.4（2026-08-12）

### 跨平台制品路径收敛
- 覆盖率制品按上传时保留的相对目录下载，PyInstaller 配置改用基于项目根的绝对入口和资源路径，修复 Linux 汇总与打包失败

## v1.11.3（2026-08-12）

### 覆盖率制品路径修复
- 覆盖率汇总任务从统一制品目录下载并合并分片数据，修复 CI 因迁移输出路径后找不到 coverage 数据的问题

## v1.11.2（2026-08-12）

### 仓库工作流收敛
- 开发、CI、发布和维护入口按职责归入 `scripts/`，测试缓存与构建产物统一写入 `.artifacts/`，避免项目根目录持续积累临时文件
- 本地发布门禁拆分为 fast、full 和 all 三档，完整测试不再重复执行核心测试，缩短日常提交反馈时间
- 审核并清理本地历史分支与 worktree，业务数据保持原位，清理证据与可恢复副本保存在数据备份目录

## v1.11.1（2026-08-12）

### 跨平台版本资源加载修复
- PyInstaller 仅在 Windows 构建时加载 PE 文件版本资源模块，修复 Linux 质量门禁因没有 pefile 而无法解析打包配置的问题

## v1.11.0（2026-08-12）

### Windows 应用进程归组
- Windows 启动器、Web 服务、runtime-worker 与计算子进程共享 QuantMaster 应用身份，并由根 Job Object 统一持有，任务管理器可按一个应用树查看，关闭根进程时不再遗留后台子进程
- PyInstaller 产物从 `quantmaster.release` 读取唯一版本源并写入 EXE 文件属性，避免安装包版本与运行时版本漂移

## v1.10.6（2026-08-12）

### 跨平台 EXE 科学计算模块修复
- PyInstaller 显式收集 SciPy array-api 兼容子模块，修复 Linux 单文件产物执行深度 doctor 时缺少 `numpy.fft` 兼容模块的问题
- 本地发布门禁对单文件产物新增隔离数据目录下的 deep doctor 冒烟，不再只验证 CLI help

## v1.10.5（2026-08-12）

### 干净安装依赖修复
- 将 rotation 核心分析直接使用的 SciPy 纳入正式运行依赖，修复从 wheel 全新安装后 `qm doctor --deep` 因缺少 scipy 无法启动的问题
- 本地发布门禁新增隔离 wheel 安装、CLI help 与深度 doctor 冒烟检查，在提交前覆盖 GitHub 的干净安装路径

## v1.10.4（2026-08-12）

### 本地 CI 与静态类型门禁
- 新增 `tools/local_ci.py`，统一运行 Ruff、异常/复杂度策略、mypy、核心与 full 分片测试，并可扩展 Chromium、Rust、wheel 和 PyInstaller 检查
- `main` 发布提交在 pre-commit 阶段自动运行完整本地门禁；失败会阻止提交和推送，门禁固定使用项目 `.venv` 并自动发现 Windows SDK
- 为运行时任务、worker 监督器、数据缓存和轮动状态补齐精确类型契约，修复跨平台异常与文件锁类型来源，恢复完整 mypy 质量门禁

## v1.10.3（2026-08-12）

### Linux IPC 关闭竞态修复
- runtime-worker 本机命令通道以确认式关闭握手消除 Linux 竞态；复杂度门禁同步到 1.10.1 已有的实际审计清单，不把修复误判为新增复杂度

## v1.10.2（2026-08-12）

### 跨平台 CI 与质量门禁修复
- Windows 独占监听测试在非 Windows runner 使用稳定常量，Unix 本机命令通道在数据目录过深时改用短临时路径，避免跨平台测试失败
- 设置诊断测试接受明确的凭据冲突响应，并清理全仓库 Ruff 质量错误，恢复完整测试、浏览器流程和正式打包审计

## v1.10.1（2026-08-12）

### 资讯异常边界修复
- 资讯租约心跳与释放仅吸收可恢复的 SQLite 故障，其他程序错误会继续暴露；流水线锁在中断时仍保证释放，不再扩大宽泛异常捕获

## v1.10.0（2026-08-12）

### LLM 请求后台隔离
- 资讯采集与重分析、Quant Lab、个股分析、自动化对话和设置检测统一改为可查询、可取消、可恢复的持久任务；浏览器请求不再等待模型网络调用
- LLM 配置与资讯标注配置各自使用持久 revision 围栏；保存设置会立即逻辑取消旧任务，迟到的模型响应不能再发布资讯、建议、消息或检测结论
- 新增全局与资讯域的 LLM 诊断、FIFO 取消检查和 HTTP 请求层保护；设置保存、模拟盘和本地缓存读取在模型排队或执行时保持可用
- 设置热应用与模型检测分离为独立后台队列；Windows Credential Manager 异常统一转换为可处理的凭据错误，读取公开设置不会再隐式写入凭据库

## v1.9.0（2026-08-12）

### 资讯扫描频率设置
- 快讯、官方公告和定期资讯默认扫描间隔分别调整为 20 分钟、2 小时和 6 小时，减少无效上游请求与重复调度
- 设置中心新增三项扫描间隔，支持 5–1440 分钟；保存后配置会立即同步到独立 runtime-worker 并重排下一次运行，无需重启服务

## v1.8.3（2026-08-12）

### 资讯增量与批次落库修复
- 快讯增量回补在来源游标缺失时使用已确认发布时间和来源新鲜度窗口作为可验证下界，不再持续把数月前历史快讯灌入待处理队列
- 快速、官方、周期及手动资讯流水线共享跨进程单飞锁；每个 LLM 批次返回后立即独立提交，已成功批次不等待同轮其他请求
- 数据库暂时繁忙不会再增加模型失败次数或送入死信；租约心跳会重试瞬时锁冲突，抓取审计批次也会合并上游重复文章身份

## v1.8.2（2026-08-12）

### 资讯 LLM 结果落库修复
- 资讯并发标注在读取租约前先取得 SQLite 写权并按批次串行提交；模型已正常返回的结果不再因延迟事务锁升级冲突丢失或误入死信
- 成功结果和失败记账的写事务使用有界 30 秒等待，多个标注线程会等待前一批原子提交，而不是把数据库竞争算作模型失败

## v1.8.1（2026-08-12）

### Windows 重载端口隔离修复
- Windows 热重载监督器改为独占 Web 监听端口；已有实例运行时立即报告端口占用，不再与旧代次共享端口后误报启动超时

## v1.8.0（2026-08-12）

### 全局页面可用性根治
- Web 代次重载改为有界蓝绿切换：新代次就绪后再排空旧代次，Windows 使用独立 Job Object 清理超时进程树，并持续记录代次心跳与饱和告警
- 每个 Web 代次通过私有排空事件停止，不再向共享 Windows 控制台广播 Ctrl+C；手动和代码重载默认在 2 秒静默窗口、5 秒最小间隔内切换，独立 Worker 不会被误中断
- 页面型市场读取只返回已发布的本地不可变快照；快照陈旧时继续展示内容并标记状态，冷启动明确返回 `snapshot_unavailable`，不再无限等待或把页面读取升级为在线刷新
- 市场总览复用已校验的快照指针、内存载荷和预编码 JSON；并发读不会重复哈希、解码或序列化大面板，发布新快照时仍原子失效并切换
- `liveness`/`readiness` 改为事件循环内的纯内存探针；数据根目录仅由 runtime-worker 显式创建，冷启动健康检查不会再悄悄创建目录或把未就绪状态伪装为可用

### 刷新任务与资源边界
- 数据刷新、取消和重试经本机 Worker IPC 执行，Web 进程只读状态并提交命令；Worker 不可用会快速返回结构化错误，而不会阻塞页面请求
- Worker Supervisor 发布启动失败诊断并以有界退避自动拉起替代进程；SQLite 首次迁移在写锁内复查版本，避免并发快照构建触发 schema 竞态
- 全局配置加载采用代次安全提交，慢速凭据/配置加载不能覆盖刚切换的数据根；本机命令管道不可用时 Worker 保持快照与读取服务，并明确报告写操作不可用
- Provider 调度收敛为固定的 Tushare、外部来源和 StockDB 线程池，并设全局网络并发上限；相同上游 lane 串行化，避免超时请求堆积为无限线程

## v1.7.0（2026-08-11）

### 全板块增量刷新运行时
- 市场、行业、题材、ETF、盘后扫描与资讯刷新统一采用版本化输入指纹、单飞任务和内容寻址产物；相同本地输入会复用已完成结果，不再重复全量计算
- Web 进程只提交和读取任务，独立 Supervisor 负责租约、心跳和 Windows 子进程计算；快照读取与 CPU/远程刷新彻底隔离
- 轮动列表改为 SQL 索引分页与不可变快照指针，题材聚合使用批量稀疏矩阵；ETF 元数据读路径取消长文件锁等待

### 快照可观测性与资讯读模型
- 请求与 DAG 节点记录耗时、缓存命中、输入输出规模和远程调用；新增离线刷新 benchmark 与硬性 no-op 回归预算
- 资讯采集改为后台统一 job，stats 与 event-focus 在写入/标注后物化；页面 GET 只读已发布统计快照，支持 ETag、cold 状态和后台进度

## v1.6.22（2026-08-11）

### 资讯影响时间归属修复
- 消息面因子统一按资讯发布时间归属交易日；抓取、正文更新和分析完成时间只作为审计证据，不再把历史事件移动到处理日
- 已归档且后来完成处理的资讯会在重新计算时自动回填原发布时间；研究特征契约升级为 `news_v2`，旧版特征缓存不再复用

## v1.6.21（2026-08-11）

### ETF 资金证据覆盖门禁
- ETF 资金评分在已知证券目录覆盖不足 80% 时不再发布，避免用稀疏的最新份额样本与完整历史样本直接比较
- 证据快照披露实际/预期 ETF 数量与覆盖率，历史窗口在当前证据不足时标记为不可比

## v1.6.20（2026-08-11）

### ETF 资金证据统一
- 市场温度的 ETF 资金证据复用 stockdb 已验证的 ETF 日线价格，补齐份额缓存中价格尚未落盘的最新交易日
- 市场温度继续使用 RotationStore 的份额观察，但不再把独立价格副本当作权威来源；快照来源明确记录 `local:stockdb:etf_daily`

## v1.6.19（2026-08-11）

### 2026-08-10 行情传播与证据门禁修复
- 默认日线目标统一解析为最近已完成交易日；StockDB 成功事件显式携带目标日，盘后、轮动、正式选股与模拟交易按交易日幂等传播
- 统一 98% 覆盖率与 OHLCV 验收门禁，前复权核对升级为稳定比例基准的 v2 契约，并对正式选股池和模拟账户重建完整来源链
- 轮动长计算增加独立租约心跳，ETF 元数据改用固定字段 v2 哈希并隔离旧 artifact；更新器等待进程树和数据目录稳定后再重启与重复验收
- 模拟交易只自动重试 `market_data_unavailable`；其他策略或执行失败继续保留人工恢复状态

## v1.6.18（2026-08-11）

### ETF 研究计算性能修复
- ETF 日线、复权、份额与交易日历改为全表一次预处理并按代码复用；不再对每只产品重复排序、合并、全表筛选和日期解析
- 前台研究刷新不再为复权缺口串行等待数百次 `fund_adj`；优先使用 stockdb 与本地缓存，缺口明确降级到收益率链或短周期原价
- 真实 1,629 只 ETF 样本的趋势与份额核心计算由超过 180 秒降至约 4 秒；首屏证据检查由约 4 秒降至约 0.02 秒

## v1.6.17（2026-08-11）

### ETF 研究首屏稳定性修复
- ETF 首屏只返回母集数量、覆盖率、来源与降级说明；完整成员证据继续保存在不可变快照中，不再把千余条目录明细塞进 overview
- 自动补算先核对本地证据文件与快照生成时点，未变化时直接复用规范哈希，并单独指纹化本地元数据源；不再每次打开都解析百万行复权缓存或误触发扫描

## v1.6.16（2026-08-11）

### 市场温度历史情绪口径修复
- 市场温度的当前值与历史变化窗口使用同一快照生成时点的资讯证据；收盘前发布、盘后完成标注的资讯不再在历史比较中显示为零条
- 研究与回测调用仍默认使用严格 PIT；看板快照明确披露资讯证据可见时间，避免把看板回溯口径误当成研究时点数据

## v1.6.15（2026-08-11）

### ETF 未收盘日期回退修复
- ETF 当前扫描先使用 stockdb 已验证交易日，盘中不再用当天自然日强求尚未存在的收盘证据
- 当前研究的不可变 Tushare ETF 目录无法精确复验时，明确降级到 stockdb 与本地缓存母集继续生成；显式历史回放仍保持严格拒绝
- 研究日来源、降级原因与本地母集一并进入快照能力、告警和内容哈希，不把不完整目录伪装成完整证据

## v1.6.14（2026-08-11）

### 资讯证据时间披露
- 市场温度的资讯证据项现在保存证据可见时间，前端可区分资讯发布时间截止与后续完成标注的时间

## v1.6.13（2026-08-11）

### 资讯情绪时间口径修复
- 市场温度将资讯发布时间截止与证据可见截止分离；目标交易日收盘前发布、但在快照生成前完成正式抓取和标注的资讯不再被误判为零条
- 研究与回测默认继续使用严格 PIT 截止时间，盘后运行的市场温度额外披露证据可见时间，避免修复实时口径时引入历史未来信息

## v1.6.12（2026-08-11）

### stockdb 高覆盖验收修复
- stockdb 更新验收不再把 100% 单源完整度当作成功条件；目标日证券覆盖率达到 98% 且已返回行情全部通过 OHLCV 校验时即可完成更新
- 未覆盖标的会以结构化警告和样本保留，继续提交盘后扫描及市场温度刷新，由本地缓存与 Tushare 等混合来源补齐；用户仍可随时再次更新
- 低于 98%、目标日尚未到达、零截面或行情字段损坏仍会明确阻断并按原有策略重试，避免把大面积缺数误判为成功

## v1.6.11（2026-08-11）

### stockdb 与市场快照解耦修复
- stockdb 已取得目标交易日且返回行情字段完整时，即使其自身仍缺少近期上市证券，也会触发市场温度的严格混合补全，不再让快照日期停在上一日
- stockdb 单源完整性状态继续按全市场分母严格验收；盘后扫描仍只接受完整成功事件，市场快照补不齐点时目录中的全部证券时仍拒绝发布
- 原生 stockdb SDK 的连接类异常会在数据源边界统一归一化；本地服务短时不可达时，研究适配器可继续进入 Tushare 缺口补齐路径
- 按 `trade_date` 精确查询的官方停牌响应允许在后续日期审计冻结；仍要求采集时间晚于目标日收盘，并保留原始响应与真实采集时间

## v1.6.10（2026-08-11）

### free-stockdb 多平台托管恢复
- 托管服务不再修改新版 free-stockdb 可执行文件；多平台引导程序原地替换 Windows 运行版后会自动再次启动，恢复本地数据库连接
- 供应商当前把打开官网作为服务启动契约的一部分；拦截该动作会使数据库端口无法就绪，因此仍随服务启动保留该页面

## v1.6.9（2026-08-11）

### 市场快照跨日衔接修复
- 证券目录观测时间与成员有效日期分离；完整 L/D/P 生命周期响应可在次日审计重建上一交易日成员，同时保留真实采集时间且拒绝含糊生命周期
- free-stockdb 目标日验收成功后会自动提交市场温度增量刷新，使本地数据库更新、`stock_bars` 发布与前端快照日期保持衔接

## v1.6.8（2026-08-11）

### 当前证券目录稀疏日期修复
- 官方退市分区缺少生命周期日期时仍可证明观测日不在市；提供了日期时继续校验先后顺序与观测日边界，不虚构具体上市或退市时间
- 港股等辅助目录的稀疏或滞后生命周期字段不再阻塞 A 股与 ETF 权威分母；目标日成员按当前状态与生命周期日期的保守交集计算
- 当前上市分区可冻结已公布但尚未到 `list_date` 的基金，目标日成员计算仍严格排除尚未上市产品

## v1.6.7（2026-08-11）

### 旧内置资讯来源恢复
- 恢复东方财富快讯、中国证监会要闻和深交所通知公告的当前官网适配器；发布时间、官方正文与原始响应继续按严格证据契约保存
- 旧内置来源的启停状态可跨重启保留，已保存来源的测试操作会直接调用内置采集器；金十开放平台仍保持需授权且默认停用

## v1.6.6（2026-08-10）

### free-stockdb 监督线程修复
- 原生 `stock_sdk` 自定义异常会转为明确的验收失败，不再以未处理异常终止后台线程
- 控制命令和监督循环均隔离单次故障；连接超时不再终止心跳或造成永久失联

## v1.6.5（2026-08-10）

### 市场温度证据链修复
- 港股证券目录允许从稳定的 `ts_code` 恢复代码，股票与港股暂停上市分区也可冻结成功空响应，不再因可选列或合法空集拒绝整份权威目录
- 当前上市分区缺少 `list_date` 时只允许证明快照观测日成员，历史读取仍拒绝回推，兼顾新上市基金的真实响应与点时边界
- 历史退市行缺少 `list_date` 不再阻塞当前 active 分母，但仍要求可验证且不晚于观测日的 `delist_date`，不虚构其历史上市起点
- 市场行情同步会先补齐当天不可变证券目录，再发布最新完整横截面；历史缺失证据仍明确拒绝，不用当前目录倒填过去
- 旧版行业缓存与真正的哈希损坏分开识别，不再周期性误报完整性告警；旧缓存继续只作降级预览，待完整新快照通过后才替换

## v1.6.4（2026-08-10）

### 行业证据异常隔离
- 行业证据降级响应改为固定公开说明，不再把严格加载异常文本传播到市场、选股、决策工作台和因子检验接口；完整堆栈仅保留在本机日志

## v1.6.3（2026-08-10）

### CI DNS 隔离修复
- 移除非 HTTPS 正文绑定测试中重复且依赖公网 DNS 的 SSRF 断言；私网解析仍由紧邻的独立测试显式注入 RFC1918 地址验证，生产 URL 边界保持不变

## v1.6.2（2026-08-10）

### CI 日期夹具修复
- 行业映射测试把合成证券目录与默认生效日固定在同一市场日，避免上海午夜换日后误用真实运行日期并触发无关的完整性失败；生产证据门保持不变

## v1.6.1（2026-08-10）

### 代码扫描安全加固
- 分钟频率只接受系统支持的固定档位，不再对任意长度的外部文本执行正则匹配；非法频率继续明确标记为不可用
- ETF 正式快照只接受内容寻址标识，快照与分钟证据路径统一限制在可信数据目录内，临时文件名不再继承请求参数
- 市场状态、每日选股、决策工作台与因子检验失败时仅返回稳定公开说明；底层异常与可能包含的路径、凭据继续只写入本机日志

## v1.6.0（2026-08-09）

### 行情真实性与 StockDB 主链
- 日线、分钟线、面板和快照接口统一返回 `data`、`quality` 与 `provenance`；交易日、分钟桶、标的范围、观测时点、单位、复权、重复和 OHLCV 语义任一不可证明时均明确降级或拒绝
- StockDB 保持价格主源；Tushare 只为 StockDB 缺少的单位、复权、证券目录、交易日历和停牌证明补证，并要求逐日逐字段完全覆盖，不能用 95%/98% 统计门把最新缺证数据标成可信
- 行情质量和分段来源链写入缓存元数据并传播到 API、流式响应、界面和正式保存门；旧缓存仅补尾部可信记录时，未覆盖历史继续 fail closed
- 全市场摄取使用独立目录、日历和停牌制品作为分母，有限值、正价格、OHLC 关系和非负成交量为硬门；未知缺失不再解释为停牌或完整截面

### 点时研究与可恢复决策
- 上海市场 15:00 成为证券目录、CSI 成分、行业、ETF 元数据、新闻分析、策略部署和正式决策的统一截止时点；次日取得的证据不能回填前一日
- 证券目录、停牌清单、行业快照、ETF 日线和复权、Champion 事件与正式决策输入改为内容寻址证据；文件、清单、逻辑哈希或时点不匹配时拒绝读取
- 研究湖只发布具有目标日不可变目录和逐日停牌证明的完整截面；StockDB 局部数据仅用于本地补齐，复权因子与行情标的集合必须一对一且有限为正
- 正式决策冻结候选母集、行情质量和来源、行业、实际特征输入与组件分数；API、CLI 和自动化共用保存前证据流程，历史回放不再读取未来更新后的当前行
- ETF 模型契约升至 V3.5，live 与 historical 目录都绑定不可变权威制品；旧快照只读取自身冻结的行情和复权证据，当前缓存变化不会改写历史产品详情

### 三类消息源与原始证据
- 快讯、官方、定期三组均有实际内容来源：新浪 7×24、上交所、人民银行、国家统计局两类 RSS 和发改委；官方详情正文与 Feed 原始响应按内容寻址保存
- committed/pending/cursor 水位、后续页 304、置顶旧条目、提供方 ID、修订、未来发布时间和分析可得时点都有反例门禁；批量情绪面板按逐条可得时间进入对应交易日
- 正式消息因子要求官方内置来源、可校验正文范围、发布时间、分析时点和可恢复 gzip 原始证据；新浪及自定义非官方来源只作提示，不进入可信因子
- Clash fake-IP 只对冻结的内置来源域名开放，IP 字面量和自定义源继续受 SSRF 私网拦截；国家统计局内置 Feed 使用独立 8 MiB 上限，自定义来源保持 5 MiB
- 金十开放平台仅加入需要授权与凭据的禁用适配位，不宣称已经接入；来源覆盖、质量、缺失范围、证据链和正式快照保存状态均在界面显式展示

### 分析优先的显式预览层
- 完整且数值有效、但单位或复权证据尚未达到正式门槛的 StockDB 行情可继续用于 API、CLI、自动化和 Quant Lab 的降级分析；正式决策、模拟执行、研究发布与模型晋级仍只接受正式证据
- 研究横截面新增 `preview_date` / `qm data preview`，只返回权威分母与已观测数据的交集，隔离重复和母集外标的，并披露 expected、observed、missing、coverage 与字段来源
- ETF 新增显式 `tier=sandbox` 的本地预览；可复用现有证券主表、元数据、份额与 StockDB 日线，但结果只驻留进程内并带 `preview_id`，不会写 production latest/history 或 pin 制品
- 短历史消息面可在 sandbox 使用 PIT 可得的正式新闻和低权重新浪快讯，输出样本期、交易日覆盖与不可晋级原因；production 的 1038 个交易日、70% 覆盖和原始证据门槛不变
- 现有旧 JSON 候选池和旧行业映射恢复当前 sandbox 分析与管理页可见性；它们不能历史回放或保存正式决策，在候选管理中重新保存后会升级为 v2 时间哈希快照
- 数据契约、PIT 制品、预览编排与主线 ETF 安全复用新增的 C901 入口已逐项纳入精确复杂度库存 169；门禁仍是只降不升的 ratchet，后续新增复杂入口会直接阻断 CI

## v1.5.2（2026-08-09）

### Tushare 无权限端点清理
- 已确认无权限的 `dc_index`、`etf_basic` 与 `etf_share_size` 在任务层直接进入现有降级链，不再重复进入数据源边界或累加本地拦截噪声；Token 变化后仍允许一次正常能力验证
- 移除当前代码已不再使用的 `ths_index` 历史健康记录；后台诊断新增“远端失败 / 本地拦截”计数，明确区分真实 Tushare 请求与零配额消耗的本地跳过
- 资金历史图的回归契约同步为当前单一金额坐标轴，不再由已淘汰的双轴缩放断言阻塞真实界面验收

## v1.5.1（2026-08-09）

### ETF 真实数据链路修复
- 修复 Pandas 空值被序列化为字符串 `nan` 后污染规范化指数的问题；同指数代表产品重新按真实指数分组，stockdb 复权位置不再误标为官方来源
- 元数据变化只重做分类与研究聚合，复用标的、日期窗口与行情内容一致的本地日线摄取，不再无意义地重读三年行情
- overview 与产品列表只返回质量摘要，完整字段诊断保留在覆盖率接口；真实环境实测 overview 176,287B、50 行产品 58,711B、最大板块 50 行下钻 66,600B

## v1.5.0（2026-08-09）

### ETF 板块热度与位置雷达
- ETF 研究页由全量数据陈列改为结论优先的日终工作台；首屏直接展示研究日与五层证据新鲜度、最强板块、低位机会、主要风险、趋势 × 位置地图和状态队列
- 建立境内宽基、策略、行业主题、海外权益、债券、商品、货币两级分类；同指数仅由代表 ETF 参与板块价格聚合，趋势、活跃度和位置分别公开，不合成统一总分
- 份额与金额按份、万份、亿份及元、万元、亿元自适应；真实零明确解释为已确认无净申赎，缺失、过期和未覆盖不再转换成伪零
- 新增板块下钻、独立产品库分页、键盘地图、焦点恢复、窄屏全宽抽屉和 200% 缩放适配；旧 ETF 快照契约直接淘汰，不保留兼容层

### ETF 功能链路与证据降级
- “更新研究”一键同步全市场元数据、最近 25 个交易日份额与复权证据后重算；可选接口失败保留警告并继续发布本地日线趋势，只有日线缺失或结构损坏才阻止发布
- 长期位置覆盖不足时整张资产地图统一降级为 60 日阶段口径；新增动量热门、阶段低位和阶段高位活跃候选，并展示尚缺的严格确认条件
- 修复创业板50、深证50、科创创业50、指数公式后缀、中英文括号别名和港股子主题分类；份额证据按连续交易日解释，低覆盖、震荡及货币产品不再生成误导性的资金确认或背离
- 修复更新后停留旧快照、资产切换隐形筛选和产品下钻丢失；产品 API 使用轻量投影，板块成员按指数分页，单只分钟线仅在趋势标签打开时按需读取

### ETF 本地证据链修复
- ETF 复权首先读取 stockdb 的累计因子事件并按交易日向前展开，无事件产品以本地权威事件表确认因子为 1；仅在本地证据不可用时调用 Tushare `fund_adj` 补缺，修复稀疏事件表被当成 0% 日频覆盖的问题
- 元数据先从 stockdb 证券主表建立完整全市场目录，再用 `etf_basic` / `fund_basic` 增强跟踪指数、管理人、托管人和费率；页面将本地可用覆盖与官方增强覆盖分开呈现，不再把可研究的本地元数据误报为 0%
- Tushare 健康状态按接口隔离；`etf_basic` 或 `etf_share_size` 权限不足不再误停 `fund_basic`、`fund_share` 和交易日历等仍可用接口，并分别呈现官方目录与指数增强覆盖
- 真实本地验收覆盖 1,617 只研究产品：1,564 只具有可核查复权序列，研究覆盖 96.72%，其中 1,228 只可计算 250 日位置；其余产品因本地日线窗口不足而明确缺失
- 仓库规则固定 stockdb 与本地缓存优先；同时明确 MAJOR 版本只能由软件所有者单独授权，功能更新使用 MINOR，修复和补丁仅使用 PATCH

## v1.4.2（2026-08-09）

### Quant Lab 1.4 发布门槛校准
- 全仓分支覆盖率门槛由 70% 校准为当前实测 69%；保留完整测试、数值 sanity checks 与覆盖率报告，不为提升一个百分点补充与 Quant Lab 数值可信度无关的测试

## v1.4.1（2026-08-09）

### Quant Lab 1.4 发布前向修复
- 收窄数据补齐、研究周期和策略 API 的预期异常边界，失败仍返回结构化原因，同时不扩大仓库宽泛异常捕获面
- 七周期研究标签使内置研究目录确定性增加到 66 项；同步目录契约，并恢复发布接口只返回最近 10 条记录，避免首屏响应随历史版本增长

## v1.4.0（2026-08-09）

### Quant Lab 1–30 日组合信号
- 研究协议统一为 `1/3/5/7/10/20/30` 日独立证据与 30 日 purge；候选只在开发区间比较，按候选×周期统一 BH-FDR、截面相关性去重以及 3–8 成分、5%–35% 权重约束构建组合，密封集只评估一次
- 历史回测与每日信号共用 T 日收盘生成、T+1 开盘执行的逐日目标仓位内核，计入方向性交易成本、换手、停牌和涨跌停约束；新增 Shadow Challenger → Paper → Champion → Degraded/Retired 生命周期及不可理由覆盖的硬门槛
- 新增七周期策略、收益曲线、晋级和工作台 API；核心图直接使用密封账本中的年化扣费后超额收益、bootstrap 95% 区间、净 IR、回撤、换手、成本和样本数，动作层输出买入、加仓、持有、减仓、退出或人工复核
- SQLite 仅新增升级到 schema v9，保留既有因子、验证、审批和决策证据；现有批准因子作为种子重新复验，不自动部署，不允许一个周期的通过结果授权另一个周期
- 本地 1,577 标的 CSI800 快照截至 2026-08-07，冷读实测 7.404 秒、同进程命中 0.169 秒；真实五因子×七周期周度复验由 788.6 秒降至 173.4 秒，峰值内存保持在 3 GiB 内，网络调用为 0
- 当前真实种子池在 1 日仅 2 个因子通过，3–30 日虽有 5 个通过但相关性去重后不足 3 个；工作台明确显示该拒绝原因，不拼接或部署虚假的多因子组合
- 消息面因子在入队前检查本地 PIT 标注跨度；历史不足时返回 `FEATURE_HISTORY_INSUFFICIENT` 及可操作的 409，不再运行到中途才误报内部错误，量价因子研究保持可独立运行
- 数据预检新增标的级扇区健康矩阵、完整缺口区间和远端请求量估算；用户可显式选择 Tushare 或 stockdb-online 仅补齐列出的区间，研究任务本身仍不会隐式联网
- 每日影子评分只读接入本地真实持仓账本，以信号日可得收盘价核算当前权重，并使用 CSI800 参考分布校准池外持仓；行情、因子评分或账本不可信时转人工复核且不会因目标暂缺而强制退出

## v1.3.99（2026-08-08）

### Quant Lab 研究快照判定修正
- 局部个股区间缺口不再把日期最新且文件齐全的本地快照误判为 `incomplete`；因子挖掘保留覆盖告警但可以继续运行
- 生产资格继续以实际在池价格覆盖率判断，停牌或上市边界不再被一概描述成必须补数

## v1.3.98（2026-08-08）

### Quant Lab 复杂度审计同步
- 将本地快照、`read_many`、统一预检和流式 GPU 训练新增的五个复杂编排入口纳入既有 C901 审计库存，基线由 110 校准为当前实测 115；不为静态门禁重写已完成数值对齐与实机验收的路径
- 复杂度盘点固定传入 `--no-cache`，受限 Windows 环境不再因 Ruff 缓存目录不可写而把库存误报为 0，并与 Linux CI 得到一致结果

## v1.3.97（2026-08-08）

### Quant Lab 发布门禁修复
- Lab SQLite v8 迁移前在线备份统一使用共享连接工厂，保持 WAL、维护屏障和确定性连接回收策略一致
- 同时缺少本地数据与可选 ML 依赖时，预检优先返回 `DATASET_MISSING` / 覆盖问题，再列出依赖阻塞，用户先看到最根本的修复动作
- 收窄批量行情读取与自动研究调度的预期异常边界，不扩大仓库宽泛异常捕获审计基线

## v1.3.96（2026-08-08）

### Quant Lab 本地快照与 CUDA 研究工作台
- 数据策略新增 `local_only` / `prefer_local` / `refresh_missing`，研究默认优先使用明确 `as_of` 的本地冻结快照且不隐式联网；CSI800 按每只历史成分的实际在池区间与 120 日预热规划，退出成分不再被错误要求覆盖到今天
- BarStore 新增一次目录查询和批量列投影读取，快照按行情文件、PIT 成分与请求区间寻址并受 2 GiB 面板 / 8 GiB 特征 LRU 预算约束；实机 1,577 标的冷读 6.83 秒、热读 0.76 秒，网络调用为 0
- 所有任务在入队和重试前统一预检数据、PyTorch / Optuna / LLM / Tushare、CUDA、磁盘与生产门禁；IO、CPU、GPU、外部任务分级调度，GPU 默认单任务互斥，旧失败保留并归类为 `LEGACY_FAILURE`
- 样本管线改为 `float32` 特征 cube / memmap、日期与标的位置索引及目标数组，Ridge 流式读取最后时点，深度模型按 batch 动态切窗并启用 pinned memory、non-blocking copy、BF16 / FP16 AMP、梯度累积和 OOM 批量探测
- 训练账本记录 requested / effective device、GPU 型号、PyTorch / CUDA、AMP、有效 batch、峰值显存、耗时和 samples/s；实机 2,055,989 样本最坏内存峰值 1.818 GiB，RTX 4080 真实训练报告 `cuda:0` 与非零显存
- 新增 `/api/v1/lab/dashboard` 与 `/preflight`，列表支持游标、筛选和摘要，SQLite v8 迁移前在线备份且仅新增字段；紧凑 Doctor 不再展开整份账本，并提供 `qm lab benchmark` 零联网性能复测
- Quant Lab 界面改为数据快照 → 因子研究 → 模型实验 → 验证审批主流程，首屏直接显示数据、计算、依赖和 Worker 就绪度；按钮先展示预检，任务中心显示资源、设备、阶段、错误与修复动作，并仅在页面可见且有活动任务时轮询

## v1.3.95（2026-08-08）

### K 线关键路径提速与历史减负
- 点击标的后立即显示并无动画定位 K 线加载态；同一标的与周期使用最多 64 项的内存 LRU，日线缓存 5 分钟、分钟线缓存 30 秒，并合并并发请求、取消过时视图和阻止旧响应覆盖新标的
- 日线默认窗口由固定 2023 年改为滚动三年，MA5/MA20 改为线性计算；市场卡片 ECharts 仅在接近视口后利用空闲时间创建，避免市场流式同步与 K 线交互争抢主线程
- 默认 `BarStore` 按数据根目录复用，free-stockdb SDK 按路径、大小和纳秒修改时间缓存且客户端按线程复用；K 线序列化不再使用 `iterrows()`，超过 500ms 时记录加载、序列化和总耗时
- 删除旧 `SwingStrategy`、独立规则决策模块及无调用方的 `POST /api/v1/backtest/run`；旧模拟账户幂等迁移为 Hybrid `short_term`，未完成订单作废但账本与成交保留，旧回测保持只读且不能重试或提升模拟盘
- 删除被本日志取代的进度文档，补齐 `/.pytest-tmp*/` 与 `/.test-runtime/` 忽略规则，并清理权限允许安全删除的历史测试缓存
- 同机同一 871 行接口各测 20 次：v1.3.94 冷 P50/P95 为 48.53/100.11ms、热为 47.26/53.52ms；v1.3.95 冷降至 30.33/83.57ms、热降至 22.99/28.67ms，热 P95 下降 46.4%。本轮不引入 Rust，因为实测瓶颈位于前端调度和重复初始化而非数值计算

## v1.3.94（2026-08-08）

### Hybrid 自动仓控与现金期
- Hybrid 决策页、历史回测和模拟盘共用无未来函数的仓位计划器：先按画像上涨概率、扣费后正预期、完整 20 日波动率及稳定画像组件一致性筛选，再按“扣费后预期收益 × 概率优势 × 置信度 ÷ 波动率”自动生成非等权目标仓位
- 组合总仓位按市场基础仓位与 `min(合格数 / Top-N, 1)` 机会系数缩放；市场基础仓位低于 20% 或没有合格标的时主动空仓，横截面 10%/90% 缩尾、单票上限迭代分配和成本缓冲后的剩余资金保留现金
- 决策响应与页面新增目标总仓位、现金比例、市场基础仓位、机会系数、合格数量、状态原因及逐股定仓分解；历史验证只统计正目标权重，空仓期明确标记为无持仓验证
- 回测质量门接受有明确仓控原因的现金期和全程空仓，历史成分过滤不再把剩余标的放大为满仓；模拟盘按差异化权重生成 T+1 订单，支持 0% 清仓、受阻卖单重试、无持仓空仓周期幂等完成及旧 Hybrid 账户一次性升级

## v1.3.93（2026-08-08）

### CodeQL 数据流深度脱敏
- 设置检测凭据改用 `scrypt` 慢速派生并绑定本机凭据库随机盐，兼顾跨重启的检测结果过期判断与离线猜测防护
- free-stockdb 热应用和模拟盘策略切换不再把底层异常写入返回对象；底层细节只进本机日志，并修正 Windows 凭据库首次写入失败时的安全降级

## v1.3.92（2026-08-08）

### 市场温度加入多窗口变化
- 市场温度新增 1、3、5、20 个交易日前的同口径变化窗口；趋势、涨跌宽度、量能、ETF 资金和资讯情绪都按实际参照日重算，ETF 与资讯严格截断到当日，避免未来数据
- 温度页加入独立变化窗口、顶部升降摘要、当前实线填充与历史虚线组成的双轮廓雷达，并在五项证据条形图上以细刻度和带符号百分点展示历史位置与变化
- 历史证据不完整时保留当前雷达、隐藏历史轮廓并披露可比维度；窗口切换只重绘已缓存的市场快照，不影响行业和题材页周期

## v1.3.91（2026-08-08）

### 设置密钥指纹与 API 异常脱敏
- 设置检测结果改用保存在系统凭据库中的每安装 HMAC 密钥认证，不再落盘可供离线猜测的稳定密钥哈希；系统凭据库不可用时仅在当前进程复用随机密钥
- FreeStockDB 审计、设置快照、数据迁移与交易接口只返回稳定公开错误，底层文件路径、凭据内容和异常链仅写入本机日志
## v1.3.90（2026-08-08）

### 浏览器候选弹层回归去竞态
- 因子候选浏览器回归改从稳定关闭态验证按钮打开，再单独验证 Escape 关闭，保留原有交互覆盖并避开 Linux Chromium 原生 popover 的关闭过渡竞态
- 鼠标选中候选时先写入结果，等当前点击任务结束后再关闭原生弹层，避免弹层过早退出顶层造成点击穿透；键盘与鼠标选择现在都会稳定收起候选

## v1.3.89（2026-08-08）

### Linux 质量门与浏览器稳定性
- Windows 专用 `WinDLL` 调用补充跨平台类型说明，Linux mypy 不再把受系统分支保护的接口误判为缺失
- 浏览器回归在重开因子候选前显式等待弹层完整关闭，并为测试服务增加超时强制回收兜底，消除 Linux 焦点时序与进程退出竞态

## v1.3.88（2026-08-08）

### CI 导航回归与质量门修复
- 浏览器回归按当前“工作区 → 二级页”导航进入回测与个股分析，并使用顶部“手册”入口及“ETF 研究”新文案，消除旧顶层按钮假设造成的定位超时
- 整理 free-stockdb 实验模块与测试夹具的历史 import 顺序，恢复整库 Ruff 质量门和后续打包审计
- 拆分本次同步新增的盘后健康度、CLI 任务、正式制品引用回填和 T+1 验证等五个复杂入口；将历史 C901 基线由过时的 93 校准为同步前主线实测 110，并补齐 free-stockdb 类型标注，恢复复杂度与 mypy 门禁

## v1.3.87（2026-08-08）

### 历史验证表紧凑展开
- 历史决策默认收为单行摘要，保留日期、周期、仓位、Top 3 名称、验证状态、周期进度和等权涨跌；点击整行后在下方展开逐股代码、T+1 开盘至当前或周期收盘的价格区间及涨跌结果

## v1.3.86（2026-08-08）

### 摄取可靠性与 ETF Web 闭环
- A 股摄取严格冻结最近 180 个已完成交易日：先回溯约 300 个自然日，不足时按 90 日扩窗且最多回溯 540 日；清单保存实际交易日、日历来源、目录快照、逐字段单位/覆盖/缺失原因及一致性结果，不足时拒绝发布
- 盘后、ETF 与研究湖正式制品原子登记 `ingest_id` 引用；最近 30 份未引用清单正常保留，被正式制品引用的清单和内容块不再被回收，并可从既有快照补建、诊断丢失引用
- ETF 工作台补齐历史快照、类别筛选、单只钻取、份额语义、分钟覆盖、stale、CSV/JSON、任务取消和同规格恢复；只有直接确认或当前制品已验证的滞后份额语义才计算资金流

### V2 影子评分、板块敏感性与策略健康
- `QM_AFTER_CLOSE_V1` 保持正式口径；每份新快照并行保存 `QM_AFTER_CLOSE_V2_SHADOW` 的量价确认评分、分数组成、排名变化及候选新增/移除，不会自动替换正式候选
- 申万板块保留正式等权排名，并增加等权、成交额加权、流通市值加权下 1/5/20 日收益、相对强弱、宽度、量能、覆盖及名次变化
- 事后标签按评分版本、周期、市场状态、申万一级和过滤器签名分组；健康视图加入全市场/中证 800 基线、容量、换手、集中度和 PSI 漂移，少于 20 个样本只显示不足，V2 至少满足 60 份五日标签等硬门后才可由 CLI 人工提升或回滚

### 真实运行时契约与受控加速
- 新增 `QM_RUN_STOCKDB_CONTRACT=1` 可选真实契约，覆盖日/周/月、同步/异步、复权证据、板块层级、A 股午休分钟聚合、ETF、300 标的大批读取和片段间取消；当前本地制品实测 6 项全部通过
- MA、EMA、MACD、RSI、ATR、BOLL 的兼容结果按制品哈希保存；当前制品实测 MA/EMA/MACD/BOLL 通过，RSI/ATR 因起始缺失位置不同未获准入
- `free_stockdb_native_acceleration_enabled` 默认关闭；开启后也只调用当前制品兼容档案已通过的方法，其他方法及原生异常自动回退 QuantMaster 并披露实际路径

## v1.3.85（2026-08-08）

### 历史验证列布局校正
- 历史决策表保留原有“前三入选”列，并在其右侧新增独立“股价变动验证”列；周期状态、交易日进度、逐股涨跌和价格区间不再与入选名单混排

## v1.3.84（2026-08-08）

### 历史选股后续验证
- 决策页底部历史快照新增 Top 3 后续验证，以信号后首个交易日开盘价作为 T+1 入场价；到达持有周期后按目标交易日收盘价定格并展示逐股价格、涨跌幅和等权结果
- 未到期快照按本地最新收盘价显示当前变动及已完成/目标交易日进度；验证仅派生读取本地行情缓存，不触发联网更新，也不改写不可变的原始决策快照

## v1.3.83（2026-08-08）

### 市场风格强弱收益图重构
- 强势样本改为零线上方红色镜像柱，低位样本改为零线下方绿色镜像柱；柱长展示收益绝对幅度，悬停仍显示原始收益正负
- 强弱差改为蓝色折线，±0.25 pp 判断死区改用淡黄色背景带；图表尺寸、数据口径、当前分布和确认路径布局保持不变

## v1.3.82（2026-08-08）

### 观察页深链接统一
- 观察工作台的行情、市场温度、市场风格、轮动总览、行业周期、细分题材和 ETF 资金统一使用 `#observe/*` 深链接
- 移除旧版市场、轮动与 radar 深链接兼容；旧书签不再定向迁移，页面切换和浏览器回归只验证当前正式地址

## v1.3.81（2026-08-08）

### free-stockdb 可复现摄取与全场 ETF 研究
- free-stockdb 能力拆分为日线、日频截面、分钟线、收盘快照、证券目录、复权因子、ETF 份额和实验在线接口；SDK、原生模块、服务程序、数据代次及证券/退市/板块目录哈希共同构成制品身份，原地更新不再命中陈旧缓存
- 全 A 股盘后扫描冻结未复权行情、复权因子、证券主数据和多层板块目录到内容寻址摄取，盘后、研究湖和诊断按 `ingest_id` 复用；字段缺失、单位、覆盖、一致性、陈旧度及 Tushare 上游经 free-stockdb 分发的血缘均显式披露
- 原“宽基资金”扩展为沪深全场 ETF 分类研究，隔离宽基、行业主题、策略、QDII、债券、商品和货币类别排名，保留行情缺失项并提供分钟补充证据、份额有效日、历史快照、钻取和 CSV/JSON 导出
- 在线 Tick 与财务查询保持默认关闭，只允许单标的白名单请求，并加入并发上限、每日配额、短期缓存、五分钟熔断和 JSONL 审计；重复 BarStore 完整性错误在十分钟窗口内降为调试日志

## v1.3.80（2026-08-08）

### 消息推送布局修复
- 修复自动化消息推送双栏中 Bot 通道状态列被操作按钮挤压为零宽的问题，状态恢复单行显示，接入操作改在通道摘要下方完整排列
- 订阅复选框显式覆盖全局输入框最小宽度，恢复 14 像素控件和横排标签；桌面与窄屏展开策略时均不再出现逐字竖排

## v1.3.79（2026-08-08）

### 市场风格语义配色增强
- 市场风格页以红色表示强势占优和正强弱差、绿色表示低位修复和负强弱差、蓝色表示均衡与判断死区、黄色表示待确认；当前结构与四档分布同时保留文字和形状编码
- 强弱收益图增加方向着色柱、蓝色死区带和实虚线区分；最近 10 日确认路径增加三档淡色背景与彩色轴标签，布局和数据口径保持不变

## v1.3.78（2026-08-08）

### 市场温度十五日路径
- 右栏温度路径由最近 5 个交易日扩展为最近 15 个交易日；横轴按约每 3 日标注并保留首末日期，纵轴跟随区间自动缩放且限制在 0–100

## v1.3.77（2026-08-08）

### 细分题材重点关注卡片
- 细分题材首屏改为系统重点关注队列，用一个主卡与三个紧凑卡呈现周期评分、入选依据、代表样本、阶段和覆盖信息
- 自动关注复用轮动改善、相对收益、上涨宽度、量能和周期结构五项现有证据，不引入黑箱评分；搜索筛选与完整分页目录移至卡片下方
- 搜索无结果时仍保留重点关注项，桌面端采用主次分栏，窄屏完整重排为单列并保留详情入口

## v1.3.76（2026-08-08）

### 网页主动热更新
- 版本号弹窗新增“立即热更新”按钮，可从网页主动安全替换 Web worker，不等待自动 watcher 的静默期或五分钟限频
- 手动请求受本机边界与 CSRF 保护，并通过监督进程独立信号执行；FreeStockDB 全程保持运行，完成后页面自动刷新

## v1.3.75（2026-08-08）

### 盘后扫描数据更新入口
- 盘后扫描工具栏以“更新扫描数据”替代含义模糊的“强制重跑”，直接更新扫描所用的全市场行情库并就地反馈进度与结果
- 历史重算改为填写截止日后才出现的“按当前数据重新计算”；数据更新期间暂停扫描，失败时保留详细状态入口

## v1.3.74（2026-08-08）

### 自动化运营台结构改版
- 自动化页面重构为运行总览、任务调度、消息推送和运行记录四个页内视图，首屏集中呈现运行状态与按严重度排序的待处理项
- 任务、通道和推送目标改用紧凑数据行与渐进展开，操作结果统一使用上下文反馈；任务、事件与审计记录按需加载并支持全局刷新
- 窄屏保留完整操作和 44 像素触控区域，记录表转换为字段化纵向记录块；自动化样式与脚本加入文件修订参数以规避旧缓存

## v1.3.73（2026-08-08）

### 行业周期坐标自适应与摘要收紧
- 周期坐标根据当前层级轨迹的起点与终点动态计算横纵轴范围，为集中分布保留适量边距，不再固定从零开始压缩有效区域
- 行业摘要改为紧凑右侧栏并取消与图表强制等高；中等桌面宽度即可左右排列，窄屏仍保持纵向阅读

## v1.3.72（2026-08-08）

### 市场风格确认路径图表化
- 最近 10 日确认路径由文字状态块改为小型阶梯折线图，使用实心、空心点区分已确认与待确认，并保留强势占优、均衡、低位修复三档迁移语义
- 小图移至强弱样本收益右侧、当前分布下方；窄屏自动下排且不隐藏状态信息

## v1.3.71（2026-08-08）

### CNN 恐贪阈值说明补全
- 历史曲线的黄色虚线明确说明为 CNN 分数不高于 10 的罕见恐惧参考阈值，并补充分数越低越恐惧的方向解释
- 图内标签移到绘图区左端并完整显示，不再因右侧裁切只剩一个“罕”字

## v1.3.70（2026-08-08）

### 版本与 stockdb 信息间距收紧
- 压缩页面版本号与 stockdb 数据日期之间的行距和垂直占位；头部只保留一个已验证数据日期，验收操作时间移入独立状态窗口

## v1.3.69（2026-08-08）

### 市场温度图表布局增强
- 近五日温度路径改为右栏迷你折线图，位于四档分布下方并与温度序列主图并排，窄屏继续自动纵向排列
- 证据分解新增五维雷达图并置于证据条左侧；维度不完整时明确等待完整证据，不把缺失项误画为零分

## v1.3.68（2026-08-08）

### 行业周期层级工作台修复
- 行业周期新增一级、二级行业整页联动切换，坐标图、阶段摘要和信号矩阵不再混合两个层级；二级行业继续沿用最多 30 个关注项
- 周期坐标改为可见后初始化并过滤无效坐标，强化轨迹起点和方向；桌面端采用图表主栏与摘要侧栏，信号矩阵可在首屏直接进入

## v1.3.67（2026-08-08）

### CNN 恐贪读数间距修正
- 仪表盘分数上移、当前情绪文字下移，并为两行设置独立行高；在最矮卡片尺寸下仍保留可见间隙，不再发生文字重叠

## v1.3.66（2026-08-08）

### 设置检测状态持久化
- 设置中心的模型目录、联网搜索、Tushare、数据目录、综合数据源、研究环境和监听地址检测会保留状态、详情与完整检测时间
- 相关设置或凭据修改后，历史结果会明确提示需要重新检测；持久化文件只保存安全结果和不可逆配置指纹，不保存 API Key 或 Token

## v1.3.65（2026-08-08）

### CNN 恐贪文字层级修正
- 仪表盘当前分数适度缩小，贪婪、恐惧等当前情绪分段名称同步放大并提高字重与对比度，主次关系更均衡且保持互不重叠

## v1.3.64（2026-08-08）

### 市场温度区间语义配色
- 温度区间按冰点、拉锯、强势扩散和过热分别使用冷蓝、弱势绿、上涨红和警示黄，当前判断不再以单色展示

## v1.3.63（2026-08-08）

### 市场温度纵轴间距修正
- 温度序列图扩大右侧绘图区安全间距，使纵轴分层文字与纵向缩放条保持清晰分隔，不再互相重叠

## v1.3.62（2026-08-08）

### 盘后扫描过期提示澄清
- 盘后扫描页明确区分专用 free-stockdb 与 Tushare、页面行情缓存；数据日期不一致时直接说明当前日期、要求日期和仍在展示的正式快照
- 未发布提示新增可执行的恢复说明和本地行情库设置入口，等待真实交易日验收通过后再引导用户重新扫描

## v1.3.61（2026-08-08）

### stockdb 数据时效独立入口
- 页面版本号下方仅显示 stockdb 已验证交易日并定时刷新；最近验收时间保留在独立状态窗口中
- free-stockdb 官方动态迁入 stockdb 专属状态窗口；QuantMaster 版本窗口只保留应用发布日志，两个入口和未读状态彼此独立

## v1.3.60（2026-08-08）

### CNN 恐贪切页动画重播
- 每次从其他工作台或观察子页重新进入行情页时，CNN 恐贪指针与数字都会从 0 正向扫到当前分数，不再只在整页刷新后播放
- 重复点击当前行情页不会无故重播，快速切页会取消过期动画；减少动态效果模式继续直接显示最终状态

## v1.3.59（2026-08-08）

### free-stockdb 启动官网彻底抑制
- 确认原生 daemon 模式仍会强制打开供应商官网后，sidecar 改为按发行包哈希生成无启动页托管副本；原始 `stockdb.exe` 保持不变
- 托管副本只清除无条件启动页入口，在线版本检测与更新提醒继续保留；无法可靠识别的新发行包会停止托管并明确报错，不会回退到弹窗启动

## v1.3.58（2026-08-08）

### CNN 恐贪指针方向修正
- CNN 恐贪指针改用与仪表盘刻度一致的 210 度至负 30 度角度映射，当前分数不再指向相反色段
- 首屏动画从左下方 0 点沿刻度正向扫向目标分数，继续使用缓起、中段加速、末段缓停的非线性节奏

## v1.3.57（2026-08-08）

### 行业与题材周期评分校准
- 行业周期改用与市场温度一致的趋势向上口径，合并强势加速与趋势延续，坐标和轮动变化不再因强势加速样本稀少而塌缩
- 行业与题材新增随 1、3、5、20 日窗口变化的透明周期评分，以 75% 绝对结构和 25% 同层级相对证据合成，并披露有效权重
- 轮动算法升级为 `QM_ROTATION_V6`；旧快照按现有启动升级流程自动重建，无需迁移用户偏好或缓存数据库

## v1.3.56（2026-08-08）

### CNN 恐贪卡片密度优化
- 观察行情的 CNN 恐贪卡片移除顶部重复分数，当前值只在仪表盘内保留一次；标题、状态、更新时间与来源压缩为紧凑信息行
- 仪表盘和历史图降低垂直占用，情绪分段边界数字同步放大，在更紧凑的卡片内保持清晰可读

## v1.3.55（2026-08-08）

### CNN 恐贪非线性入场动效
- 首屏针体和数字改用 640 毫秒的缓起、中段加速、末段缓停曲线，从 0 连续过渡到目标分数；减少动态效果模式仍直接显示最终状态

## v1.3.54（2026-08-08）

### 轮动总览表格列宽优化
- 轮动总览的行业与题材变化卡片按内容重新分配六列宽度，压缩数值列和单元格留白，常见桌面宽度下不再需要横向滚动
- 卡片表格改用固定列布局，板块名称和阶段保留更多空间；极窄内容使用省略显示，避免撑宽整张卡片

## v1.3.53（2026-08-08）

### 模型联网搜索 SSE 兼容
- OpenAI-compatible 网关即使以 SSE 返回 Responses 搜索结果，设置检测和个股研究也能从完成事件与引用事件恢复来源，不再误报网络故障
- 兼容网关的证据搜索改用轻量请求，不继承高推理强度；正式分析的推理配置保持不变，诊断同时区分网络、格式及失败或不完整事件

## v1.3.52（2026-08-08）

### CNN 恐贪指针同轴动画修正
- CNN 恐贪仪表盘的针体、尾部和中心圆环改为复用同一个真实旋转中心，不再因两套偏移坐标产生歪斜
- 首屏指针与数字从 0 连续扫到当前分数；减少动态效果模式仍直接显示最终状态

## v1.3.51（2026-08-08）

### 主站热更新五分钟限频
- 任意两次后端热重载至少间隔 5 分钟；冷却期间收到的多轮 Python 修改会统一积压并合并为下一次重载
- 停止编辑 30 秒后才提交变更，并可通过环境变量分别调整静默、批次和最小重载间隔；HTML、CSS 和 JavaScript 刷新页面仍可直接生效

## v1.3.50（2026-08-08）

### 市场温度横轴刻度加密
- 温度序列的一年默认视窗将时间横轴提高到约 12 段，日期标注更接近逐月分布；缩放后仍自动调整并避免标签重叠

## v1.3.49（2026-08-08）

### CNN 恐贪仪表盘精修
- CNN 恐贪仪表盘改为更克制的外圈分段刻度、上移轴心和独立大号分数读数，指针、数字与情绪标签保持稳定的光学间距
- 共享图表层支持显式的低频状态动画，首屏指针和数字动画不再被全局静态策略覆盖，并继续尊重减少动态效果设置

## v1.3.48（2026-08-08）

### 四档分布布局与文案修正
- 市场温度右侧四档分布进一步收窄，标签、进度条和数字使用固定列轨道，四条图形不再随数字宽度错位
- 分布说明改为每只股票只归入一档、四档合计等于参与计算的股票总数，移除难懂的家数守恒表述

## v1.3.47（2026-08-08）

### 观察 K 线横轴独立缩放
- 观察行情底部的日线与分钟线 K 线在 Ctrl 加横向滚轮时，仅缩放时间横轴，不再因数据过滤联动重算价格纵轴范围
- 横轴内部缩放与底部滑块统一使用不影响纵轴范围的窗口模式，成交量横轴仍与主价格图保持同步

## v1.3.46（2026-08-08）

### 市场温度主图空间扩展
- 市场温度主图在桌面端使用更宽的不对称双栏，右侧四档分布适度收窄并保留完整数值；窄屏继续自动切换为单列

## v1.3.45（2026-08-08）

### 观察行情涨跌悬停数据修正
- 观察行情的当日涨跌图悬停浮层按 ECharts 分类轴返回的 `dataIndex` 回查日期，标题不再始终显示为破折号
- 区间涨跌兼容分类轴直接返回的数值格式，不再因误判为日期数值数组而固定显示 0.00%

## v1.3.44（2026-08-08）

### CNN 恐贪分段刻度与入场动画
- CNN 恐贪仪表盘按 0、25、45、55、75、100 的情绪等级边界显示坐标，不再使用均匀四等分刻度
- 页面首次载入分数时，指针平滑扫向当前值并同步递增读数；系统启用减少动态效果时保持静态

## v1.3.43（2026-08-08）

### 市场温度默认视窗
- 市场温度曲线默认聚焦最近 252 个交易日，纵轴初始显示 0–60；底部与右侧缩放控件继续支持查看完整历史和更高温度区间

## v1.3.42（2026-08-08）

### 横向选择器纵向溢出修正
- 观察行情的自选、关注、持有选择器明确禁止纵向溢出，不再因横向滚动能力意外显示右侧上下滚动条
- 同一约束覆盖 K 线与牛熊周期、自动化强度、工作台导航、板块页签、盘后层级、Quant Lab 筛选和窄屏步骤导航等横向选项轨道；相关样式使用文件修订参数，刷新后不会继续命中旧缓存

## v1.3.41（2026-08-08）

### 设置中心样式加载修复
- 设置页样式与脚本增加文件修订参数，浏览器不再将分组后的新版页面结构与旧版资源混用
- 桌面侧栏与窄屏分区选择器使用明确网格区域和组件作用域，避免选择器意外占位后挤乱整页布局

## v1.3.40（2026-08-08）

### free-stockdb daemon 生命周期接管
- sidecar 改用 free-stockdb 原生 daemon/service 模式启动和停止，为后续无窗口托管建立独立的进程生命周期
- 后台启动器即使先于服务退出仍会等待端口就绪，并在数据更新或 QuantMaster 退出时使用原生停止命令安全关闭托管服务

## v1.3.39（2026-08-08）

### CNN 恐贪仪表盘视觉修正
- 观察行情与决策页的 CNN 恐贪仪表盘重新安排指针、轴心和分数，避免当前值互相遮挡，并收敛色带、刻度和背景层次
- 全球风险背景不再显示原始 ISO 时间串，改为浏览器本地时区的月日与时分，并区分实时更新和本地缓存

## v1.3.38（2026-08-08）

### RSI 悬停数据点几何修正
- 观察行情 RSI 的悬停数据点按图表实际横纵缩放反向校正，宽卡片中保持为正圆，不再被拉成椭圆
- 悬停点屏幕半径缩小并固定为约 1.4 像素，同时收细描边，减少对曲线和附近数值的遮挡

## v1.3.37（2026-08-08）

### 市场温度证据补全
- ETF 资金接入近 5 日净申购率的 252 日历史百分位，使用连续份额快照并严格对齐行情日
- 情绪代理接入截至行情日 15:00 的质量加权资讯；完整本地数据下证据权重达到 100，轮动算法升级为 `QM_ROTATION_V5`

## v1.3.36（2026-08-08）

### 观察行情 RSI 三个月交互
- 观察行情的日线 RSI(14) 仅绘制截至最新交易日的最近三个自然月，不再固定展示 90 个交易日
- 鼠标悬停 RSI 曲线会吸附最近交易日，并以定位线、数据点和浮层显示完整日期及 RSI 数值

## v1.3.35（2026-08-08）

### 主站热更新进一步节流
- Python 修改停止 30 秒后才重载；持续编辑时最多每 5 分钟合并为一次重载，减少主站与 Bot 的重复启停
- 可通过 `QM_RELOAD_QUIET_SECONDS` 和 `QM_RELOAD_MAX_BATCH_SECONDS` 调整节流窗口；HTML、CSS 和 JavaScript 仍然刷新页面即可生效

## v1.3.34（2026-08-08）

### 跨市场日线时区兼容
- yfinance 等海外行情源的带时区日线统一按交易所本地日期保存，已有 parquet 缓存读取时也会自动兼容
- 行情历史内部读取失败返回服务暂不可用，不再将缓存或上游异常误报为标的不存在

## v1.3.33（2026-08-08）

### 观察行情日线 RSI 日期轴
- 观察行情卡片明确标注 RSI(14) 为日线指标，并在曲线下方显示首、中、末交易日期
- RSI 曲线按历史点的真实日期间隔定位，不再丢弃接口已经提供的日期信息

## v1.3.32（2026-08-08）

### 市场温度稳健分层
- 市场温度改用无未来数据的对称滞回状态机，持续趋势不再因短期波动频繁退出，也不使用全历史极值缩放
- 市场风格、行业周期与题材轮动统一复用新分层；温度区间同步为冰点/黄金坑、拉锯、强势扩散和过热，图表默认聚焦最近 252 个交易日

## v1.3.31（2026-08-08）

### 轮动二级页窗口参数修正
- 细分题材与宽基资金分页接口正确解析浏览器传入的 1、3、5、20 日观察窗口，不再将合法窗口误判为 422
- 切换观察窗口继续按对应周期信号排序；不支持的整数或文本参数仍会被明确拒绝

## v1.3.30（2026-08-08）

### 主站热更新降噪
- Python 文件连续写入会等待 3 秒静默，并以 15 秒为最长批次合并为一次 worker 重载，避免保存多个文件时反复启停主站与 Bot
- `release.py` 的发布记录变更不再单独触发重载；HTML、CSS 与 JavaScript 仍然只需刷新浏览器即可生效

## v1.3.29（2026-08-08）

### 数据源检测语义修正
- 真实行情探测区分可达、限流、认证、权限、上游异常与网络故障；HTTP 429 不再被显示为成功
- Windows 套接字访问受限归入可恢复网络故障，不再因错误文本中的“访问权限”被永久停用

## v1.3.28（2026-08-08）

### 设置中心信息架构重排
- 设置侧栏按 AI 与连接、数据、研究与执行、资讯和系统分组；数据配置拆为本地行情库、在线数据源与研究数据三个任务页
- 检测按钮与摘要、耗时和可展开详情就地呈现；相关配置变化后保留旧结果并提示重新检测，窄屏改用分组选择器

## v1.3.27（2026-08-08）

### 事件聚焦控件与滚动间距统一
- 事件聚焦窗口改用与行业周期完全一致的轴线和短下划线控件，移除独立的灰色分段底板
- 板块情绪与标的提及滚动列表统一增加右侧内容安全区，数值不再紧贴滚动条轨道

## v1.3.26（2026-08-08）

### 事件聚焦筛选加载修正
- 修正事件聚焦合法时间窗口被查询参数校验拒绝的问题，1、3、7、30 日现在均可正常加载
- 资讯样式与脚本增加文件修订参数，浏览器不再将新版页面结构与旧版资源混用

## v1.3.25（2026-08-08）

### 事件聚焦短周期筛选
- 资讯事件聚焦新增 1、3、7、30 日滚动窗口，默认展示近 7 日；切换只更新标的提及排行，不影响事件流、大盘情绪和板块分
- 排行继续按首次入库时点、内容去重和质量门槛统计；快速切换、空数据和请求失败均提供稳定反馈与就地重试

## v1.3.24（2026-08-08）

### free-stockdb 无人值守更新与安全回收
- 到点后由监督进程自动停库、运行可见的原生更新器、恢复服务并验证全市场真实交易日；失败最多重试三次，只有验证通过才提交盘后扫描
- 监督进程与热重载 worker 通过 SQLite 控制邮箱共享命令和真实状态，设置页持续显示目标日、实际日、验证结果和重试进度
- 启动窗口关闭或父进程退出时回收本次托管的 stockdb；再次启动仅回收身份和路径均可验证的遗留进程，外部托管进程保持不动
- 观察页明确“同步观察行情”只更新本页、自选和参考标的缓存，不更新全市场 free-stockdb

## v1.3.23（2026-08-08）

### 飞书重复断连清理
- QuantMaster 完成飞书私有 WebSocket 清理后解除旧传输引用，避免 SDK 公共 `stop` 再次从活动事件循环执行 `run_until_complete`
- 热更新退出不再产生 `private ws shutdown raised` 或 `Client._disconnect` 协程未等待警告

## v1.3.22（2026-08-08）

### 模型地址与凭据绑定修正
- 同一兼容网关的根地址、`/v1` 地址和完整接口地址改用同一凭据槽；补全 API 版本路径不再导致模型列表检测丢失 API Key
- 根地址的聊天路由返回网页或不存在时安全回退到 `/v1`；非 JSON 响应显示 HTTP 状态、内容类型和经过脱敏的页面标题或文本摘要

## v1.3.21（2026-08-08）

### 飞书孤儿缓存协程回收
- 识别 `lark-oapi` 在执行器线程内为 `ExpiringCache` 创建的独立未运行事件循环，并在热更新退出时通过专用收尾线程消费任务取消状态
- 缓存清理协程不再因游离于 WebSocket 循环之外而触发 `Task was destroyed` 或 `coroutine was never awaited` 警告

## v1.3.20（2026-08-08）

### 资讯分析错误原因可见
- 失败项和暂停项直接说明模型超时、连接失败、鉴权、接口地址、网关响应或 JSON 格式等具体原因，并给出对应排查建议
- 已有记录中的笼统错误会根据结构化错误码即时显示为可理解的原因

## v1.3.19（2026-08-08）

### 暂停项手动恢复不限次数
- 手动恢复不再受自动恢复的三次上限和退避时间限制；后台自动恢复仍保留时间窗口与最多三轮保护

## v1.3.18（2026-08-08）

### 飞书热更新安全退出
- 主站热更新前关闭飞书自动重连，并依次取消和等待心跳、消息接收、过期缓存等 SDK 后台协程，再结束其私有事件循环
- 正常的 WebSocket 1000 关闭不再触发 pending task、协程未等待或事件循环提前销毁警告

## v1.3.17（2026-08-08）

### 热更新启动日志恢复
- 热更新 Web worker 重新安装与 `qm` CLI 相同的终端及轮转文件日志，启动就绪、自动化、Bot、Lab 与安全退出信息重新可见
- `--verbose` 状态由监督进程传递给每一代 worker，热重载后不再静默降级

## v1.3.16（2026-08-08）

### 轮动样式缓存修正
- 轮动样式改用文件修订参数并强制重新验证静态资源，确保窗口选择器不会继续使用含纵向滚动条的旧 CSS

## v1.3.15（2026-08-08）

### 轮动窗口纵向溢出修正
- 桌面端的轮动观察窗口不再创建滚动容器，彻底消除 1、3、5、20 日切换器的纵向滚动条；仅窄屏保留隐藏轨道的横向滑动

## v1.3.14（2026-08-07）

### 轮动窗口控件修正
- 轮动的 1、3、5、20 日观察窗口保留窄屏横向滑动，同时隐藏意外显示的滚动条并禁止纵向溢出

## v1.3.13（2026-08-07）

### 候选日期选择器重建
- 候选历史日期改为项目内月历弹层，彻底移除浏览器原生日期输入的上下微调和滚轮行为

## v1.3.12（2026-08-07）

### 候选日期控件外观修正
- 保留日历选择能力，移除浏览器原生日期输入可能显示的上下微调器，并继续禁止滚轮误改日期

## v1.3.11（2026-08-07）

### 发布质量修正
- 修正数据研究模块的变量遮蔽，登记资讯抓取故障边界，并收紧发布文案格式以通过发布前 Ruff 检查

## v1.3.10（2026-08-07）

### Windows 进程品牌化
- 一键启动自动生成基于项目 `.venv` 的 `QuantMaster.exe`，主站监督进程和热更新 worker 在任务管理器中不再显示为 `python.exe`
- 命名启动器嵌入项目标识、QuantMaster 产品信息与当前发布版本；正式 PyInstaller 构建同步使用同一图标

## v1.3.9（2026-08-07）

### 图表滚轮语义修正
- 普通滚轮沿对应轴平移已缩放的数据窗口；仅按住 Ctrl 时沿该轴缩放，图内 Ctrl 滚轮会拦截浏览器页面缩放

## v1.3.8（2026-08-07）

### 主站安全热更新
- Windows 一键启动默认监视主站 Python 代码并安全替换 Web worker，热更新过程不再启停 FreeStockDB
- HTML、CSS 与 JavaScript 改动刷新页面即可生效；退出整个启动器时仍完整清理主站并停止其托管的 FreeStockDB，避免后台进程残留

## v1.3.7（2026-08-07）

### 数据图表轴向滚轮缩放
- 在可缩放数据图内，纵向滚轮缩放纵轴；横向滚轮或 Shift 加滚轮缩放横轴，页面其余区域保持正常滚动

## v1.3.6（2026-08-07）

### 候选列表宽度与日期交互修正
- 候选日期输入不再响应鼠标滚轮；成员表按可用宽度分配列宽，消除中等宽度下的无效横向滚动与留白

## v1.3.5（2026-08-07）

### 页面内选择器统一
- 时间窗口、行情周期与推送强度等互斥选择器统一为二级导航的底部蓝色当前位置标记，取消圆角底色

## v1.3.4（2026-08-07）

### 观察页签选中态收口
- 观察二级页签取消圆角底色，改为文字强调与底部蓝色当前位置标记，悬停不再产生按钮式背景

## v1.3.3（2026-08-07）

### 资讯暂停项手动恢复修复
- 连续失败的资讯统一显示为“已暂停”，界面不再暴露内部队列术语
- 手动恢复不再受 1/3/7 天自动恢复窗口限制；仅在恢复次数已用尽或任务正在执行时禁用，自动任务仍遵守原退避窗口

## v1.3.2（2026-08-07）

### 观察导航层级优化
- 观察工作台将市场、轮动、信息改为明确的静态分组轨道；页面按钮与类别标题在间距、字重和分隔上清晰区分

## v1.3.1（2026-08-07）

### 一级入口导航修正
- 自动化改为直接进入一级工作台，不再显示重复的二级菜单；手册和设置保持无上下文栏

## v1.3.0（2026-08-07）

### 导航与市场轮动精修
- 顶部重组为“观察、选股、研究、交易、自动化”五个工作台；各工作台在会话内记住最后页面，并兼容旧市场、轮动深链到规范地址
- 市场风格、行业周期、细分题材和 ETF 资金补充持续性、阶段迁移、资金极值与连续申赎摘要；题材和 ETF 明细改为完整服务端分页
- 轮动算法升级至 `QM_ROTATION_V3`，旧快照标记待升级并由本地后台原子重建；页面切换不再重播图表或逐行入场

### 资讯标注独立并发
- 设置中心的“资讯处理”新增 1–16 路独立并发设置，默认同时处理 4 个标注批次；通用模型任务继续沿用自己的并发上限
- 资讯标注使用独立 FIFO 队列并并行认领批次，不再挤占交互分析槽位；原子落库、租约续期、流式进度、失败退避和暂停项恢复保持不变

## v1.2.3（2026-08-07）

### 行情缓存旧文件名迁移
- 自动识别安全规则升级前将 `=` 替换为 `_` 的行情缓存；仅在目录归属唯一且内容哈希匹配时原子迁移，避免误报文件缺失和无效联网重拉
- 缓存经迁移校验或完整重写恢复后，自动关闭对应的旧修复告警并记录审计事件

## v1.2.2（2026-08-07）

### 盘后扫描任务重提修复
- 盘后扫描只合并仍在排队或执行中的同规格任务，历史失败任务不再被“运行全市场扫描”按钮重复取回
- 已完成、失败或取消的扫描均可创建新任务；快照层继续通过输入哈希保证同一份研究结果不可变

## v1.2.1（2026-08-07）

### 盘后扫描真实数据兼容修复
- 兼容 free-stockdb SDK 指定字段后的二维位置数组响应，真实全 A 股日频截面不再被误判为空
- 板块成员关系采用“板块代码＋成分代码”复合缓存键，同一股票属于多个板块时可以完整写入研究湖
- 历史决策接口显式解析查询字符串中的持有周期，修复合法 `horizon=3` 返回 422

## v1.2.0（2026-08-06）

### free-stockdb 盘后研究引擎
- 全 A 股盘后扫描新增最新交易日、证券/OHLCV 覆盖、板块目录与覆盖骤降完整性门；通过后发布带输入哈希的不可变快照
- 申万一级优先级可展开二/三级与概念板块，研究候选逐项披露趋势、相对强弱、流动性、量价、风险、估值/规模可用性及排除规则
- Web、CLI 与 API 共用统一任务和快照，支持取消、恢复、历史重放、CSV/JSON 导出；原生更新失败时沿用旧快照并推送原因
- 正式截面与点时板块成员关系进入研究湖，1/3/5/7 日标签加入全市场超额、回撤、容量、换手和集中度；结果不自动下单或提升为交易建议
- 中证800点时成分通过 Tushare `index_weight` 拉取并按生效日缓存到研究湖，盘后验证与 Quant Lab 复用；缺少权限时显式降级而不阻断快照

## v1.1.4（2026-08-06）

### 交易接口异常信息收口
- 回测、交易与模拟盘接口不再向浏览器返回底层异常文本；服务端保留诊断日志，客户端仅收到稳定状态码和不含内部路径、凭据的安全提示

## v1.1.3（2026-08-06）

### 项目验证环境规范
- 仓库规范明确要求使用项目 `.venv` 执行 Python 验证；完整测试在受限 Windows 环境可指定工作区内的 pytest 临时目录

## v1.1.2（2026-08-06）

### 生产回测 PIT 数据复用
- 生产级回测与 Quant Lab 共享本地 PIT 成交数据集；已完整归档的历史区间不再逐标的重复请求 Tushare
- PIT 缓存保留未复权成交价、复权信号、涨跌停与停牌约束，并沿用原子写入和完整性校验；当日数据仍按收盘刷新规则重新核验

## v1.1.1（2026-08-06）

### free-stockdb 无打扰启动
- 托管服务显式使用 `stockdb.conf` 进入服务器模式，不再在每次启动时自动打开 free-stockdb 官网
- QuantMaster 低频读取官网中的更新摘要、数据日期和最新版本；内容变化时在版本按钮显示提示点，并在现有更新日志浮层中展示详情
- 顶部“帮助”入口更名为“手册”，并同步换用书本图标

## v1.1.0（2026-08-06）

### 资讯处理模型分级
- 资讯处理可独立选择模型和推理强度，推理强度提供从无到最大的完整七档；轻量定时标注无需再复用通用分析模型，留空资讯模型时仍自动跟随全局设置

### free-stockdb 深度集成
- 自动发现托管目录中的 stock_sdk，本地首次缺库使用 SDK 批量接口；公共体验源只参与小批交互补缺，不进入全量刷新和后台任务
- 连接、超时和 HTTP 状态异常均可继续回退后备数据源；设置页展示实际 SDK、服务、更新阶段和最近结果
- 手动更新由 sidecar 安全停止并恢复本地服务；当前发行包没有已验证的静默参数，因此保留原生窗口，定时任务改为应用内更新提醒

### 图表界面收口
- 移除各类图表右上角的重播按钮，保留标签切换时的自动动画和品牌片头重播

## v1.0.0（2026-08-06）

### 首个正式探索版
- 完成本轮稳定性、数据源、模拟盘、可观测性和测试技术债收口，进入 1.x 前向迭代
- 固化当前用户事实数据与公开接口基线；0.x 问题不再维护，修复只通过新的 SemVer 版本发布
- 继续定位为探索级量化研究工具，不承诺生产可用性，所有输出均不构成投资建议

## v0.21.1（2026-08-06）

### 诊断契约同步
- 同步模拟盘 schema v2 的深度诊断断言，恢复完整 CI

## v0.21.0（2026-08-06）

### 恐贪与 RSI 趋势仪表盘
- CNN 恐贪指数新增当日指针仪表盘和近一年历史曲线，弱网时继续使用最近成功缓存
- 主要指数与其他市场卡片新增醒目的 RSI(14) 数值、独立曲线和 RSI 22 参考线
- 决策页集中对比候选等权大盘与全部当前板块的 RSI 曲线，并强化板块低 RSI 提示
- 模拟盘账户栏收拢控件；策略、候选和调仓频率可直接修改并按 15:00 分界排入后续交易日，净值横轴固定使用日期

## v0.20.0（2026-08-06）

### free-stockdb 托管运行时
- 可选托管用户自行安装的 free-stockdb 完整发行包，随 QuantMaster 启停并按设置的盘后时间增量更新
- 目录、托管、自动更新和时间均可调整；不捆绑程序、数据或同步源，失败时继续使用后备数据源
- 长时间更新可随应用退出而终止，运行状态进入诊断与设置热应用结果

## v0.19.0（2026-08-06）

### 市场指标与候选版诊断
- 大盘卡片和决策板块新增日线 RSI(14)，CNN Fear & Greed 作为明确标注的美国/全球风险背景
- RSI<22 显示“加仓抄底观察”，同时 CNN<10 显示“罕见大底机会”；弱网时使用短缓存或显式降级
- `/api/v1/diagnostics` 与 `qm doctor --deep` 新增所选主源、各市场优先级和数据源能力矩阵，读取过程不会探测网络
- 架构文档同步 capability 扩展契约，明确 free-stockdb 默认但可切换、且不随 QuantMaster 捆绑
- Windows 一键启动会自动复用或拉起用户放在 `runtime/free-stockdb` 中的本地 `stockdb` 服务，文件未就绪或连接失败时返回明确警告并继续使用后备数据源

## v0.18.2（2026-08-06）

### 证券名称单一存储
- 运行时停止读写 `stock_names.json` 兼容缓存，名称查询、刷新和手工保存统一落到 `security_master.sqlite`
- 旧 JSON 仅保留一次性迁移读取，既有用户名称不会丢失
- 仓库忽略本地 uv 与隔离测试临时目录，开发状态不再被缓存污染

### 数据注册类型修复
- 日线与分钟线写入后显式校验可读结果，并分离面板的日线和分钟存储类型，修复完整 mypy 门禁发现的 3 项问题

## v0.18.1（2026-08-06）

### 统一数据源能力
- 日线、分钟线、实时快照、指数成分、行业和题材改为显式能力；调度器会跳过不支持当前请求的来源
- free-stockdb 继续作为可切换的默认首选，并将基础接口、注册器和适配器纳入 mypy 检查

## v0.18.0（2026-08-06）

### 模拟盘单一契约
- 删除旧单账本 `PaperTrader`、`/paper/run`、`/paper/report` 与 `qm paper run`，模拟交易统一使用可管理的独立账户、提案、确认和开盘撮合流程
- 修复 `qm paper propose` 读取创建参数的问题，并更新当前 CLI 指南

## v0.17.3（2026-08-06）

### 技术债门禁收紧
- 复杂度存量从 94 降至 93，宽异常实际存量从 204 校准至 200；两个门禁现在都会阻止基线在代码改善后继续虚高
- 个股研究任务的静态进度分支改为数据映射，保持事件文案与进度百分比不变

## v0.17.2（2026-08-06）

### 个股研究证据边界
- 证据账本、严格 JSON、内容哈希与证据校验迁入独立模块，深度研究编排文件减少约 180 行基础设施实现
- 保留既有研究模块导入契约，并将新证据模块纳入 mypy 检查

## v0.17.1（2026-08-06）

### 模拟盘自动化解耦
- 每日模拟交易 worker、租约心跳与进程内单例迁入独立模块，账户存储和交易服务不再同时承担调度职责
- 服务端、自动化入口和回归测试统一使用新模块，并将该边界纳入 mypy 检查

## v0.17.0（2026-08-06）

### 运行时发布元数据瘦身
- `release.py` 只保留应用实际展示的最近 10 个版本，完整发布历史继续由 CHANGELOG 保存，源码从 2784 行降至约 200 行
- 版本接口与发布钩子契约保持不变，不再解析或加载 100 余份已经退出支持范围的历史 Python 字面量

## v0.16.1（2026-08-06）

### Windows 一键启动
- 仓库根目录新增 `qm-serve.cmd`，固定使用项目 `.venv` 中的 Python 启动服务，避免 Conda base 或旧版全局 `qm` 命令干扰

### 数值测试去重
- 本地快速集从 249 项收敛到 175 项，资讯与 LLM 集成测试转入完整 lane，默认开发反馈不再加载重型边界
- 合并净值、行业中性、样本外验证、绩效和遗传因子挖掘的重复等价用例，移除未再使用的 Hypothesis 依赖

## v0.16.0（2026-08-06）

### 首轮测试瘦身
- 浏览器回归从 15 条收敛到 8 条核心产品流程，删除重复的错误恢复、动画细节和内部状态测试，减少 Chromium 重复启动
- 移除发布工作流与 Quant Lab 静态字符串镜像测试，并将帮助页验收收敛为导航、搜索、计算器和响应式主路径

## v0.15.0（2026-08-06）

### 模拟账户策略管理闭环
- 模拟账户详情明确展示策略可编辑、历史锁定或只读归档状态，产生历史前可直接编辑，产生历史后可复制策略继续探索
- 账户 API 增加稳定管理能力字段，删除继续采用可恢复归档并保留策略快照、订单与成交账本

## v0.14.5（2026-08-06）

### 健康探针异常边界
- 后台健康探针保留完整本机日志，但公开诊断仅返回稳定恢复提示，关闭最后一条异常栈暴露告警

## v0.14.4（2026-08-06）

### 代码扫描残余收敛
- 文件缓存安全字符集恢复基本面派生键与 Yahoo 期货代码使用的 `#`、`=`，继续拒绝路径分隔符和目录跳转
- 行情缓存统一经过受限路径解析，运行诊断不再向 API 返回底层异常文本

## v0.14.3（2026-08-06）

### 代码扫描安全边界收敛
- 限制 GitHub Actions 默认权限，集中加固文件路径边界，并阻止内部异常文本进入资讯、诊断、决策和板块 API

## v0.14.2（2026-08-06）

### 数据源边界收敛
- 将 free-stockdb 行业降级与诊断探针拆为独立边界，保持 v0.14 功能不变，恢复复杂度质量基线

## v0.14.1（2026-08-06）

### free-stockdb 首个可发布补丁
- 保留 v0.14 的 SDK 分钟行情与行业/概念板块接入，并将外部故障处理收敛为明确异常类型
- 恢复宽异常质量门禁，不扩大技术债基线

## v0.14.0（2026-08-06）

### free-stockdb 本地行情与板块数据
- 默认复用用户安装的 free-stockdb 成熟 SDK 与本地数据，补充前复权日线、1/5/15/30/60 分钟线、行情快照、申万行业和概念板块
- 主数据源、SDK 目录和服务地址均可设置；不捆绑 free-stockdb 程序、数据包或上游同步源，不可用时自动回退现有来源
- GitHub Release 仅展示当前版本变更，仓库 CHANGELOG 继续保留完整历史

## v0.13.54（2026-08-06）

### 静态资产测试契约修正
- 仪表盘与交易工作台测试改为从同源 `app.js` 验证脚本契约，不再假定主脚本内联于 HTML
- 保持页面结构与脚本行为分别验收，避免静态资产拆分后的错误测试阻断有效发布

## v0.13.53（2026-08-06）

### 前端资产边界与 CSP 收紧
- 仪表盘内联样式与主脚本拆分为独立同源静态资产，HTML 模板由约 281 KB 收敛到约 108 KB，职责边界更清晰
- 内容安全策略不再依赖逐请求 nonce，脚本与样式仅允许同源文件；现有动态样式属性继续受单独兼容策略约束
- 安全契约直接验证外部主脚本的 CSRF 自动续期逻辑，避免模板拆分削弱本地服务的请求保护

## v0.13.52（2026-08-06）

### 候选池保存后导航竞态修复
- 候选池写入后的目录刷新会等待既有读取结束并重新获取最新目录，确保新建候选立即加载详情并正确清除未保存状态
- 成功保存的候选不再被导航保护误判为未保存草稿，切换实盘、模拟盘等管理页面恢复稳定

## v0.13.51（2026-08-06）

### Rust 原生内核安全升级
- PyO3 从 0.24.2 升级到 0.29.2，修复 RustSec 报告的迭代器越界读取与 Python 可调用闭包线程安全问题
- 保留 cargo-audit 作为发布门禁且不添加漏洞豁免；原生内核仍由三平台一致性任务验证后进入桌面包

## v0.13.50（2026-08-06）

### 面向未来的可复现发布链
- 桌面发布只接受已存在的当前版本 tag，历史回填清单与手动重建入口已删除，既有 tag 不再由工作流创建或移动
- 三平台构建统一使用锁定环境和不可变 Action 提交；Rust 依赖新增 cargo-audit，setuptools 构建后端固定为 83.0.0
- 三个桌面产物由单一发布任务汇总，并附带 CycloneDX SBOM、SHA-256 校验清单和 GitHub 构建来源证明，避免并发覆盖发布状态
- 版本接口只返回最近 10 个面向未来的版本说明，完整历史统一链接到 CHANGELOG，应用不再加载或维护旧版本发布契约

## v0.13.49（2026-08-06）

### Python 3.12 与分层测试入口
- 源码运行环境升级为 Python 3.12+，开发、CI 和桌面构建统一固定到 3.12.13，不再承担临近停止安全维护的 Python 3.10 兼容成本
- 默认 Pytest 收敛为 250 个快速契约；重型研究、回测和数据韧性套件改由 `--full` 与覆盖率分片显式执行，本地检查不再加载全部 598 项
- 三平台核心 CI 不再重复编译和检查 Rust；原生扩展改由独立三平台一致性任务验证，Rust fmt、check、clippy 与 test 只集中执行一次

## v0.13.48（2026-08-06）

### 权威数据库版本化迁移
- 账本、模拟盘、研究目录、自动化与 Lab 统一通过 `user_version` 执行有序事务迁移，结构、回填和版本标记不再分离提交
- 历史库存异常、研究任务索引、策略告警和因子名称等一次性修复迁移成功后不再随每次构造扫描用户事实数据
- 深度诊断新增六类数据库的当前版本、期望版本和冷启动或升级状态，结构漂移可以在进入业务流程前被发现

## v0.13.47（2026-08-06）

### 资讯统计索引化
- 资讯库的历史指纹、内容哈希和分析维度只在版本迁移时回填；正常启动不再随资讯总量扫描或改写用户数据
- 标的与申万行业改为事务维护的索引化派生关系，分析更新后立即同步，行业映射变化时仍可安全重建
- 大盘、每日、行业与标的聚合全部下推 SQLite；五万条实测统计由约十六秒降到一秒内，Python 峰值内存由约 123 MB 降到不足 3 MB

## v0.13.46（2026-08-06）

### 轻量测试入口与 Node 24 CI
- 常规 Pytest 不再隐式启动 15 个 Chromium 管理流程；完整浏览器场景保留在显式 CI lane，本地需要时可用 `QM_RUN_UI=1` 单独启用
- Ruff 复杂度历史上限从失真的 299 收紧到当前 94，已经消除的复杂函数不再允许悄然回流
- CI 与发布工作流固定到 Node 24 版 checkout、artifact 上传和下载 action 的不可变提交，消除 Node 20 弃用警告并保持构建可复现

## v0.13.45（2026-08-06）

### 启动直达与窄屏设置体验
- 首次打开应用会直接进入工作台，不再强制播放 8.6 秒全屏片头；品牌标记仍可随时重播，预览和显式 `intro=1` 链接继续可用
- 窄屏设置页的分区导航、表单控件和操作按钮采用至少 44 像素触控目标；标题操作会自然换行，不再挤成难以阅读的竖排按钮
- 移动端自动保存条改为随表单流动，后台状态条进一步限制收起宽度，不再遮住输入；研究批次与个股分析状态同时清理装饰性侧边条

## v0.13.44（2026-08-06）

### 数据接口硬超时与测试客户端前向兼容
- 数据提供商任务从首次入队起共享可配置硬截止；SDK 卡住时调用方会有限等待、立即打开熔断并切换备用源或缓存，不再无限阻塞界面任务
- 已经超时的上游线程即使迟到返回，也不能把熔断状态改写为成功；诊断新增各通道 active、waiting、expired 和累计超时统计
- 设置中心、YAML 和 `QM_DATA_PROVIDER_TIMEOUT` 可管理 1–300 秒截止；开发测试锁定 `httpx2`，消除 Starlette `TestClient` 的旧客户端弃用告警

## v0.13.43（2026-08-06）

### 模拟账户策略管理
- 模拟盘账户详情新增独立策略快照、完整策略哈希、候选成员与来源信息，每个账户当前运行的策略和参数都可直接查看
- 账户支持修改名称与执行方式；没有调仓或成交历史时可原地调整策略，已有历史时引导复制并调整，避免改写历史策略语义
- 账户删除改为可恢复归档，会停止未完成自动运行并废止待处理订单，但完整保留资金、成交和调仓账本；已删除账户可随时查看并恢复

## v0.13.42（2026-08-05）

### 精简测试目录覆盖修正
- 会话级轻量证券目录自动收集当前测试引用的规范代码，并为常用标的保留名称；账本、设置、名称缓存与性质测试不再依赖完整目录导入
- 继续保持完整打包目录的独立覆盖，同时让三片覆盖率测试在隔离数据根下使用一致的当前契约种子

## v0.13.41（2026-08-05）

### 前向兼容基线与测试套件瘦身
- 兼容测试从 v0.13.39 当前数据与 API 契约起向前维护，不再让更早版本的一次性迁移和已移除路由占用日常发布门禁
- Pytest 为每个隔离测试复制一次会话级证券目录种子，不再重复导入三万余条证券及数十万别名；完整目录测试仍独立使用正式打包快照
- 相同代码路径的编码、非有限数值和主机边界改为单用例内数据表覆盖，减少重复 fixture 成本，同时保留当前输入类别和安全断言

## v0.13.40（2026-08-05）

### 市场、轮动与资讯图表增强
- 市场行情卡片悬停同时展示所选日期的区间涨跌和当日涨跌；自选、关注与持有标的会用本地证券主数据补齐名称，不再只显示代码
- 市场温度、行业周期和宽基资金图支持横纵坐标缩放；行业周期坐标按当前节点与轨迹自适应上限，宽基资金双轴按实际数据范围留出边界
- 宽基 ETF 资金冷启动会回填近三年份额与收盘价，并保留约三个交易年的可缩放序列；近期数据继续优先使用单位净值
- 资讯事件聚焦扩充到 24 个标的，逐项展示名称、代码和提及次数；卡片限制高度并支持纵向滚动，更多聚集结果不会挤长整页

## v0.13.39（2026-08-05）

### 跨平台原生门禁与弱网发布韧性
- 跨平台核心 CI 将 PyO3 显式绑定到 uv 锁定的 Python，避免 macOS runner 的较新系统 Python 越过项目版本矩阵导致原生检查失败
- 发布同步为每次 `git push` 增加 30–600 秒可配置超时，弱网连接不再让提交钩子无界等待；超时继续进入既有重试和待同步恢复流程

## v0.13.38（2026-08-05）

### CI 恢复与跨平台门禁修复
- 跨平台核心测试显式为固定 Rust 1.94.0 工具链安装 `rustfmt` 与 `clippy`，不再因 runner 默认组件差异误报失败
- Ubuntu 覆盖率分片同步 `quality` 锁定依赖，确保 `coverage` 命令在每个隔离环境中可用并继续执行 70% 分支覆盖率门禁
- 发布同步新增绑定失败 GitHub Actions run、原提交和原版本的受控恢复标记；只允许前向发布紧邻 patch，成功推送后自动清除

## v0.13.37（2026-08-05）

### 稳定化基线、并发租约与可复现发布
- 资讯事件流新增失败项重试、暂停项恢复、错误原因和实时批次进度；人工恢复绕过自动退避与健康门禁，“全部重试”固定启动时最大 ID 并逐批处理
- 可重建的 `news_analysis_claims` 在 `BEGIN IMMEDIATE` 内原子认领资讯，以随机 token、租约和心跳 fencing 拒绝旧 worker 覆盖接管者结果
- 所有 LLM 调用共享 FIFO 并发队列和 1–300 秒可配置排队超时；超时返回可重试结构化错误，并暴露 active、waiting 与 timeout_count
- 模拟盘和轮动共用官方或已验证本地交易日解析器，不再用普通工作日猜测中国节假日；无可信日历时安全跳过，夜间关闭后次日可补跑上一交易日
- 模拟盘自动运行新增随机 lease token、30 秒心跳与 fencing，最多重试 6 次后等待人工恢复；策略告警与运行告警分开保存，成交继续幂等
- 轮动工作台升级为总览、行业周期、细分题材和宽基资金四页，提供 1/3/5/20 日变化、成员收益、市场超额、宽度、量能、真实成分映射与完整贡献明细
- 市场与指数图补齐首中尾年月刻度；进入决策页只读匹配条件的历史快照，仅在用户点击生成时重新计算保存
- 发布校验区分待提交当日与历史恢复模式；v0.13.30 tag 已补建并精确指向 `04f1a43`，未补造 v0.13.31–v0.13.36 tag
- 提交跨平台 `uv.lock`，CI 与桌面发布使用锁定同步；固定 uv 0.11.32、Rust 1.94.0、PyInstaller 6.19.0、maturin 1.14.1 与 pip-audit 2.10.1
- 安全审计直接校验带 hash 的完整锁定集合，禁用 pip 二次依赖解析，避免网络抖动导致审计无界等待

### 诊断、质量门禁与界面韧性
- 诊断接口和 `qm doctor --deep` 新增资讯认领、LLM 排队、模拟盘重试、交易日日历来源及轮动五类快照质量；`QM_CONFIG_PATH` 可隔离诊断配置
- CI 拆分为跨平台核心、Ubuntu 覆盖率分片、浏览器和打包审计，统一输出 JUnit、最慢 30 项并设置测试与 job 超时；分支覆盖率门槛提升到 70%
- 轮动页签补齐方向键、Home/End、roving tabindex、ARIA 关联和可见焦点；决策完整结果会拒绝较慢的历史请求覆盖，表格入场改用纵向动效，消除窄屏与缩放下的横向溢出
- mypy 扩展到资讯、LLM、模拟盘、自动化与轮动边界；Ruff 复杂度和宽异常策略均建立只减不增的 ratchet，架构文档同步真实 worker 与恢复流程

### 连接复用与有基准保护的性能优化
- LLM 请求改用生命周期管理的共享 httpx 连接池；模型端点热切换时旧连接会等待在途请求结束后关闭，应用退出时统一释放
- 轮动总览按五类快照 ID 缓存并随新快照自动失效；资讯统计使用专用索引和 SQL 窗口去重，减少完整时间窗进入 Python 的记录数
- 性能改动补齐连接热替换、缓存失效、SQL 去重和查询计划回归测试，不降低数据质量、覆盖率或动态效果可访问性
- 统一任务首次失败后立即人工重试时，即使旧 worker 尚在清理，也会在清理完成后可靠重新调度，不再停留在等待重试

## v0.13.30（2026-08-01）

### 模型推理强度贯通
- 设置中心的模型服务新增无、极低、低、中、高、极高和最大推理强度，并按提供商约束可选级别；YAML 与环境变量也可配置同一选项
- 普通对话与原生联网搜索请求均按 OpenAI Chat Completions、Responses 和 Anthropic Messages 协议携带推理强度，不再静默遗漏

## v0.13.29（2026-08-01）

### 市场行情卡片图表迁移
- 市场行情与主要指数卡片升级为可核查的区间走势图，新增零轴、最新位置、克制的方向面积、时间范围和区间收益，不再只是装饰性折线
- 迷你图支持悬停查看日期与区间涨跌，并采用红涨绿跌的区间方向语义；卡片和折线仅在载入时错峰入场，系统“减少动态效果”下保持静态可读

## v0.13.28（2026-08-01）

### 题材受限目录持久复用
- 已完整遍历并发布的部分题材目录在上游目录未变化时直接复用，不再重复扫描数千个需要登录的同花顺详情分页
- 复用只更新暂存运行审计，不重写已发布目录或伪造观测时间；后续快照继续携带原始目录分母、`partial` 状态和去重后的降级说明

## v0.13.27（2026-08-01）

### 题材质量口径校准
- 受限题材快照的 `coverage` 以原始目录规模为分母，不再只按已发布子集计算接近 100% 的误导性覆盖率
- 题材 provider 与构建阶段重复产生的降级说明统一去重，并新增部分目录质量状态、分母和问题列表的端到端回归测试

## v0.13.26（2026-08-01）

### 题材冷启动受限目录
- 三套完整题材目录均不可用且没有旧目录时，可原子发布至少覆盖 20% 且不少于 75 个的逐项完整题材，并明确标记 `partial`；失败或不完整分页绝不混入
- 90% 完整目录门槛保持不变；已有完整旧目录时不以部分目录覆盖，已遍历的持久暂存可直接恢复，避免反复扫描需要登录的高页码

## v0.13.25（2026-08-01）

### 同花顺后备源代理隔离
- 同花顺题材作为最终公开后备源时不再继承失效的系统代理，避免代理返回 200 拦截页却缺少成分表，已完成的暂存题材仍可断点复用
- 继续保持 90% 整体目录质量门槛和原子发布，不以降低覆盖标准掩盖网络故障；新增客户端代理隔离回归测试

## v0.13.24（2026-08-01）

### 健康库迁移事务闭环
- 数据源健康库完成 v3 重分类后先提交迁移事务，再允许 provider 打开显式抢占事务，修复首次请求可能出现的嵌套事务错误
- 新增从 v2 权限记录迁移后立即发起 `provider_call` 的回归测试，确保升级首个请求稳定命中永久熔断且不会触达上游

## v0.13.23（2026-08-01）

### 数据源权限熔断纠正
- Tushare“没有接口访问权限”等真实错误文案统一归类为永久权限故障，通道直接进入 `disabled`，不再按瞬态上游故障周期性消耗请求额度
- 数据源健康库升级到 v3，启动时重分类已持久化的权限错误并原地纠正旧熔断状态；凭据变更或用户显式探测仍可安全恢复

## v0.13.22（2026-08-01）

### 全方法 Origin 安全边界
- Origin 校验提升到统一请求中间件，GET、HEAD 等读取请求与写入请求使用同一来源边界，显式跨来源请求在进入路由前即被拒绝
- 跨来源拒绝继续返回稳定的 `origin_rejected` 错误码，并新增读取请求回归测试，防止安全策略再次退化为只保护不安全 HTTP 方法
- 市场概览并发结果使用明确的返回契约，缺失项与分组状态采用独立类型收窄，消除批量行情分支的静态歧义并保持现有降级语义

## v0.13.21（2026-08-01）

### 轮动任务平滑接管与降级日志收敛
- 服务正常停机时，正在执行的轮动任务不再被误标为用户取消；worker 原子释放租约并保留运行状态，新进程可立即接管同一不可变任务
- 东方财富、Tushare DC 与 ETF 净值等预期后备切换只记录脱敏、截断的单行原因，不再倾倒链式堆栈；真正的全源失败仍保留任务事件和错误上下文

## v0.13.20（2026-08-01）

### 品牌标识与启动动效
- 采用眼睛、目标环与蓝色方形因子构成全新 QuantMaster 标识，提供标准版、反白版、组合字标和浏览器图标；页眉同步启用新标识并可点击重播片头
- 首次启动以手绘地平线、单色方形日升、眼睛非线性扫视、蓝色因子发现、方形太阳和标识收束讲述品牌，支持 Esc 跳过且每个浏览器标签页只自动播放一次
- 时间轴支持任意帧定位和录制终帧保持，动画仅使用合成友好属性；减少动态效果用户直接看到静态落版，应用内保持静音，并附独立预览页

## v0.13.19（2026-08-01）

### 安全会话与数据源韧性
- 本机边界、Origin、Host 与 CSRF 拒绝使用稳定错误码；前端统一客户端只对可恢复令牌错误续签一次，恶意来源不会被自动重试
- 数据源健康账本区分权限、鉴权、能力缺失、限流、网络和空响应；永久故障进入 `disabled`，配置变化或显式探针后才恢复，过期半开租约可安全接管
- 美元指数增加 AKShare 后备；盘中宽度拒绝伪造 50% 中性值，仅在 15 分钟内复用通过样本校验的缓存
- 批量行情与基本面故障按操作聚合为一条含数量和样本的告警，收益率计算统一禁用 pandas 隐式前向填充

### 持久自动化与资讯暂停项恢复
- 自动化任务接入统一 `jobs.sqlite`，具备不可变规格、幂等键、租约、心跳、取消、截止时间、失败事件和瞬态错误退避重试；旧 running 记录明确迁为 `interrupted_legacy`
- 资讯标注连续失败三次后暂停自动重试；每日最多恢复 20 条，按 1/3/7 日退避且最多三轮，Web 可筛选并手动恢复，失败码和恢复次数完整保留
- 飞书会话进入与入群生命周期事件安全接收，正常 WebSocket 关闭不再按异常堆栈报警；Bot 命令和后台任务使用独立有界执行器

### Lab 内存治理与回测前置门禁
- 共享多周期样本升级为 `sample-store-v3` memmap 和紧凑整数元数据，构造前估算磁盘；Ridge 使用分块充分统计量，深度训练与验证按索引分批读取
- 因子列表只在顶层逗号处分隔，排队前离线解析内置、基本面、Lab 引用和表达式；无效规格返回 422，可继续的数据门禁进入 `needs_confirmation`
- 题材目录冷启动自动提交后台构建任务并显示真实进度；数据源诊断增加显式恢复探针

## v0.13.18（2026-08-01）

### 全站图表系统与克制动效
- 市场、决策、因子、回测、模拟盘、实盘、轮动和资讯图表统一为可复用的 ECharts 主题与生命周期；切换标签页或点击图表“重播”可再次演示，并保留缩放、图例和单实例状态
- 统一蓝色主叙事、A 股红涨绿跌、琥珀阈值和紫色独立对照的受控语义色；资讯情绪迷你 SVG 迁移到同一坐标、提示和动画体系
- 高密度数据自动缩短或关闭逐项动画，系统“减少动态效果”会停用空间位移；相邻表格同步获得粘性表头、数字对齐和前十行错峰入场

## v0.13.17（2026-08-01）

### 飞书分类标题与详细正文
- 飞书自动化卡片标题收敛为重要资讯、资讯摘要、盘中变盘、收盘状态、任务异常和任务结果等固定类别，长新闻标题与错误句不再挤占标题栏
- 正文按类别补充新闻来源、事件类型、影响范围、市场指标、异常阶段、可用性影响、错误详情和重试指引，同时避免重复展示相同依据

## v0.13.16（2026-07-31）

### 新闻通知信噪比与异常上报
- 新闻拉取、分析和摘要任务成功时保持静默，只推送达到目标订阅阈值的重要资讯内容
- 来源拉取或 LLM 分析出现异常时合并为一张脱敏错误卡，并按任务、阶段、失败来源和小时去重，持续故障不再随扫描频率重复轰炸

### CSRF 会话自动恢复
- 刷新页面会替换过期或因服务重启失效的 CSRF 令牌；已打开页面遇到明确的令牌拒绝时自动续签并安全重试一次，细分题材刷新无需反复手动刷新

## v0.13.15（2026-07-31）

### 市场温度与数据新鲜度
- Tushare 空响应不再写入权威接口缓存；已有空缓存会自动失效并重拉，全市场同步可记录单日缺口后继续推进
- 轮动快照记录实际日期和最近应完成交易日，陈旧数据强制显示为过期；未发生业务变化的刷新明确报告“数据未推进”

### 全球市场逐标的容错
- 美股、港股、日经、韩国指数、黄金、原油、铜、美债十年期和离岸人民币接入已验证的新浪、AKShare 与 Tushare 后备，Yahoo 限流不再拖空整组行情
- 每个参考标的独立同步、校验和熔断；不可用标的保留名称、来源尝试及失败原因，美元指数无兼容后备时不会用不同口径数据冒充

### 轮动与题材快照韧性
- 轮动雷达和行业周期坐标按实际强弱占比选择易读刻度，低比例数据不再挤在左下角
- 题材目录增加同花顺整库后备、分页进度、断点恢复和 90% 发布门槛；暂存目录通过校验后才原子替换旧目录
- 刷新任务幂等复用，浏览器刷新后可重新附着；前台明确显示已更新、部分更新、未推进或失败，不再只有后台日志

### 资讯情绪自适应刻度
- 保留聚合分理论范围 ±100 和既有标签阈值，仪表、趋势与板块条按观测分布统一选择 ±10 至 ±100 的自适应档位

## v0.13.14（2026-07-31）

### 可选数据源缺失容错
- 最小安装未包含 AKShare 时，题材目录将其视为东方财富来源不可用并继续尝试 Tushare DC，不再因导入错误绕过统一双源容错；回归测试显式覆盖未安装状态

## v0.13.13（2026-07-31）

### 双层六维研究与可读报告
- 原深度流程调整为 2–5 分钟快速分析；默认深度分析升级为三轮受限联网追证、六维首轮研判与反方复核、交叉终审和独立证伪，最长 900 秒
- 报告协议升级到 2.1，逐维核验证据数量、来源覆盖和审查轮次；研究深度不足时明确列出缺口，并以 `completed_with_errors` 交付已有结论
- 拒绝把 LLM 返回的对象或 JSON 包装直接写入正文；Web 与飞书统一提取可读文本，飞书证据附录改用字段标签展示，不再暴露原始 JSON

### 联网搜索复探测与兼容
- Responses 联网搜索在完整请求遇到 400、415 或 422 时自动尝试最小官方载荷；失败能力缓存按网关和模型隔离，并在五分钟后重新探测
- 设置页新增“检测联网搜索”，通过服务进程已有凭据强制复探测，不返回密钥或原始上游错误；搜索不可用时仍按证据降级规则完成分析

### 渐进式研究体验
- Web 与飞书新增反方观点、待确认问题、研究深度和独立终审展示，并同步取证、反方复核和终审阶段及预计耗时

## v0.13.12（2026-07-31）

### 暗色滚动条与内容安全间距
- 页面和内部滚动区统一使用低对比度暗色轨道、清晰滑块及悬停/按压反馈，不再出现刺眼的浏览器默认白色背景
- 页面、弹层、候选列表、日志、回测明细和帮助侧栏等纵向滚动区预留稳定空间，避免滚动条覆盖文字与操作控件

### AKShare 上游修复适配
- 概念成分查询直接使用新版目录返回的 `BK` 代码，避免重复按名称解析；东方财富不可用时继续使用现有 Tushare DC 整库后备
- 深度分析显式限制新浪财务指标为近五年，避免新版默认抓取全部年份挤占证据截止时间
- 每日估值迁移到现行百度估值接口，标准化静态/滚动市盈率、市净率和总市值；无等价历史股息率时保持缺失，不再调用已移除的乐咕函数

## v0.13.11（2026-07-31）

### AKShare 版本更新
- AKShare 最低兼容版本提升至 1.18.81，数据扩展安装时直接采用上游最新正式版

### 题材双源容错与轮动质量语义
- 细分题材保持东方财富优先；连接不可用时自动尝试 Tushare `dc_index + dc_member`，两套目录整套切换并追踪来源，不混合板块代码，双源失败保留上次可用目录
- Tushare 权限型题材接口使用独立健康通道；账号没有当前所需的 6000 积分权限时，不会熔断 2000 积分档的行情、申万行业和 ETF 数据
- 未知分母不再伪装为 `0%`；轮动雷达改报实际可用维度数，细分题材冷态只显示等待快照，页面切换前的迟到请求不再覆盖当前状态栏

## v0.13.10（2026-07-31）

### 历史制品交付与跨平台收口
- 早期提交没有 `Cargo.lock` 时使用独立的非锁定 `maturin build` 命令，避免 macOS Bash 3 在 `set -u` 下展开空数组失败；有锁文件的版本继续强制 `--locked`
- 历史矩阵的 Windows、macOS、Linux 二进制改为具名 Actions 制品保留 90 天并写入任务摘要；分支触发的受限令牌不再尝试跨标签创建 GitHub Release，也不再把成功构建误报为失败
- Chromium 实盘 CSV 流程在上传文件后明确等待预览按钮可见再点击，消除页面切换尚未完成时命中隐藏表单的偶发 30 秒超时
- 恢复清单新增 v0.13.8，补齐工作流演进期间的历史三平台制品

## v0.13.9（2026-07-31）

### 最小权限发布职责分离
- 历史恢复工作流不再尝试使用受限 `GITHUB_TOKEN` 创建指向含 Actions 文件的标签；此操作需要 GitHub App 无法自授予的 Workflows 权限，改由已安装 release-sync 且具 Git 推送权限的发布操作者创建不可变标签
- Actions 只接受已存在且精确指向清单目标提交的标签；标签缺失、移动或版本不匹配时在构建前失败，三平台构建与 Release 写入继续只使用最小 `contents: write` 权限

## v0.13.8（2026-07-31）

### 版本化发布恢复清单
- 历史补录目标进入受版本控制的 `.github/release-backfill.json`；`main` 只在该审计清单或恢复工作流变化时触发，不再依赖本机凭据缺失的管理员 API 权限，修复工作流后可安全重跑未完成目标
- 发布准备阶段统一解析普通标签、手工输入和恢复清单，逐项验证完整提交 SHA、`main` 祖先关系、目标运行时版本、标签唯一性与不可变指向；缺失标签交由具 Workflows 权限的发布操作者补齐
- 构建矩阵扩展为“版本 × Windows/macOS/Linux”的笛卡尔积，并关闭 `fail-fast`；单个历史构建失败不会取消其他目标，补录 Release 明确不覆盖 `latest`
- 本次清单覆盖稳定性计划的 v0.10.1、v0.10.2、v0.11.0、v0.11.1、v0.12.0，以及发布恢复工作流自身的 v0.13.6

## v0.13.7（2026-07-31）

### 跨平台截止测试与完整矩阵诊断
- 个股深度分析的截止时间回归改用 2 秒阻塞取数桩，在验证 1 秒截止确实提前返回的同时，为不同平台的线程调度和确定性规则报告收尾保留 500ms 明确预算，消除 macOS ARM runner 的 9ms 边界抖动
- CI 测试矩阵关闭 `fail-fast`；任一系统或 Python 版本失败时，其余组合继续完成并报告结果，避免首个偶发失败取消五个平台并遮蔽真实跨平台状态

## v0.13.6（2026-07-31）

### 可审计的历史发布恢复
- 桌面发布工作流新增受限的历史补录入口：目标标签与完整提交 SHA 必须同时提供，目标提交必须属于 `main` 历史，且标签必须与该提交的运行时版本完全一致
- 已有标签只能复用原提交，工作流拒绝移动不可变标签；缺失标签必须由具备 Workflows 权限的发布操作者创建，避免在 CI 内保存无法自授予权限的高权限密钥
- 历史源码使用当前可靠的 wheel 构建路径生成 Windows、macOS 与 Linux 制品；目标提交存在 `Cargo.lock` 时强制 `--locked`，早期提交没有锁文件时显式执行可审计的非锁定构建
- 补录 Release 明确不覆盖 `latest`；版本不符、目标不是 `main` 祖先、制品缺失或原标签指向不同提交时立即失败

## v0.13.5（2026-07-31）

### SQLite 句柄生命周期与 Windows 原子切换
- 统一 SQLite 连接工厂现在保证上下文退出时先提交或回滚、再确定性关闭数据库句柄，消除 254 处调用依赖垃圾回收时机产生的连接积压和 Windows 文件锁
- 数据根迁移的只读源库与暂存目标库使用显式 `closing`，并通过规范 file URI 打开源库；完整性校验结束后句柄已释放，Python 3.12 Windows 可可靠原子重命名暂存目录
- 新增连接关闭契约与 Windows 迁移回归，迁移失败断言直接输出后台错误，避免截断任务字典掩盖真实原因

### Windows 受限解释器边界
- Windows 虚拟环境中的受限 Python 直接启动基础解释器并显式继承当前导入路径，避免 venv 或 uv 重定向器占用唯一的 Job Object 活动进程名额
- Job Object 的活动进程上限继续保持为一：合法因子 worker、输出与超时检查可以正常运行，用户代码创建子进程仍由操作系统拒绝

### 浏览器回归稳定性
- 设置页自动保存验收改为等待稳定的 `saved` 状态，不再依赖异步切换的提示文案，消除快慢 Chromium runner 间的偶发竞态

## v0.13.4（2026-07-31）

### 六维联网取证与交叉复核
- 个股分析默认进入最长 300 秒的深度模式，并保留快速模式；基本面、技术面、消息面、资金面、市场心理与宏观政策并发取证，六维分别完成规则计算和受限 LLM 研判后再交叉终审
- 证据按官方/结构化数据、AKShare、可信媒体的固定顺序采集；每条证据包含来源层级、URL、发布日期、数据时点、内容哈希和血缘，技术面新增 MA120/250、沪深300及行业相对强弱、突破与多周期量价，消息面计算公告后的 1/3/5 交易日价格反应
- OpenAI-compatible Responses 与 Anthropic 支持原生 Web Search，最多三轮受限搜索；能力探测按网关缓存，不支持或失败时自动回退内置金融数据源，且 Web Search 降级只进入 diagnostics、不影响 ready
- 六维各自使用最长 45 秒的结构化调用，终审最长 60 秒，进程级并发上限为 2；LLM 只能引用输入中的 evidence ID，非法引用、无依据主张和非有限数值会被拒绝并回退确定性规则

### 统一任务、Web 与飞书渐进交付
- 注册 `market.stock_analysis`，通过 `/api/v1/market/stock-analyses` 提交与读取；不可变规格、`Idempotency-Key`、尝试、租约、心跳、单调事件、取消、重试、进程恢复和同规格检查点全部接入统一 Jobs 与 SQLite 运行时
- Web 默认深度模式并可切换快速模式，固定六维顺序逐维渲染完整结论与可点击引用，展示阶段、耗时、预计剩余和取消操作；刷新后从本地 job ID 恢复，后台分析不依赖浏览器连接
- 飞书仅在用户明确发送“快速分析”时采用快速模式；进度卡原位更新并节流到单任务最多 10 次，28 KB 主卡始终保留六维结论，超量证据按编号附录完整发送
- 飞书 `message_id`、会话路由、投递状态和附录游标进入 Automation outbox 并以 job ID 关联，服务重启后继续更新原卡和未完成附录，不发送 HTML 文件或依赖公网报告页

### 严格产物、完整性与安全降级
- 规范化证据、逐维结果和最终报告通过统一任务产物接口保存为严格 JSON，校验 schema、有限数值、证据引用、内容哈希与数据血缘；只复用校验通过且属于同一任务规格的检查点
- AKShare 证据缓存损坏时立即拒用并保存带 SHA-256 的隔离清单，通过现有统一数据修复队列审计；分析同时尝试其他可信上游，不建立平行缓存或修复内核
- 单来源、原生搜索或单维 LLM 失败时仍交付确定性规则结果并标记降级；达到 300 秒截止返回 `completed_with_errors` 与已完成内容，取消和维护中断可及时停止并安全恢复
- 删除旧个股流式 API，不保留兼容别名；继续只支持 A 股研究，不读取或发送持仓、凭据，不执行交易，也不新增公网 Web 暴露

## v0.13.3（2026-07-31）

### 跨平台运行边界与 CI 完整性
- 开发测试依赖显式包含 SciPy 与 scikit-learn，六平台矩阵和覆盖率任务不再依赖 runner 是否偶然预装可选包，Ridge、Spearman 与研究诊断契约在干净环境中可重复执行
- 发布同步按 `Asia/Shanghai` 业务时区判断实际日期，UTC runner 在北京时间凌晨不再把当天版本误判为未来日期
- POSIX 受限进程逐项安全降低可用资源上限，并把 OpenBLAS/OMP/MKL/NumExpr 固定为单线程；Linux 继续限制子进程数量，macOS 使用有界进程组清理且不再在 `preexec` 阶段失败
- 历史模拟账本迁移改用 SQLite 在线备份，原子读取主库和 WAL 中已提交成交且不改写源库；设置页 Chromium 验收等待真实配置加载完成后才开始编辑，消除快慢 runner 竞态

## v0.13.2（2026-07-31）

### 持续质量与浏览器门禁修复
- 受控 mypy 按 Python 3.12 解析依赖类型存根，同时继续用真实 Python 3.10/3.12 矩阵验证运行兼容性；补齐 Windows 文件锁和 Job Object 的跨平台类型边界
- 适配新版 Ruff 对联合类型成员顺序的检查，避免托管 runner 使用最新质量工具时出现仅在远端暴露的静态检查失败
- Chromium 验收全部改用符合严格 CSP 的定位器断言和显式异步等待，修复设置、帮助、运行诊断、健康问题及确认对话框中的竞态、瞬时断言和 Promise 永久等待
- 浏览器套件默认继续在 Ubuntu CI 执行，并提供显式的跨平台本地开关，Windows 可运行同一套完整 Chromium 回归

## v0.13.1（2026-07-31）

### 跨平台发布修复
- CI 与桌面发布不再使用要求已激活 virtualenv/Conda 的 `maturin develop`；统一构建原生 wheel，再从本地 wheel 目录安装并验证 Rust 研究内核
- 修复 GitHub 托管 runner 在 Install 阶段缺少 `VIRTUAL_ENV` / `CONDA_PREFIX` 而退出的问题，Windows、Linux 与 macOS 使用同一条可复现安装路径

## v0.13.0（2026-07-31）

### 可恢复数据、自修复与发布一致性
- BarStore 与 ResearchLake 现在显式区分缺失、孤儿、哈希不符和损坏 Parquet；读取会报告完整性状态，写入使用暂存文件、内容哈希、父目录同步以及进程内可重入、跨进程互斥的单标的锁，避免并发覆盖和掉电丢失更新
- 新增持久数据修复队列并接入 `/api/v1/jobs?domain=repairs`：不可变规格、幂等键、尝试与事件、租约/心跳、取消/重试齐全；自动修复按来源限制并发、指数退避和每日额度，校验通过才原子替换，原件进入带 SHA-256 清单的隔离区
- 文件缺失、哈希不符、孤儿文件及不可读研究分区不再静默跳过；可读旧孤儿缓存以明确 `orphaned` 降级状态离线使用，其余异常进入修复队列并保留审计记录
- 因子特征面板按已验证原始交易日重建索引，缺失因子日期保留为 NaN；Quant Lab 模型预测先写耐久 staging 和 outbox，再幂等发布到研究湖，发布暂时失败时训练结果保留为 `completed_with_warnings`
- 历史未配对卖出迁移为 `unknown_cost_sell` 异常并从已实现收益剔除；新账本继续拒绝超卖、NaN、Infinity 和非法金额，消除零成本虚增收益

### 市场温度与板块联动
- 市场页新增“行情 / 市场温度 / 市场风格”二级导航，顶层新增“轮动”，包含轮动雷达、行业周期、细分题材与宽基资金；六个视图复用暗色交易工作台、A 股红涨绿跌、ECharts 与窄屏交互语言
- `QM_ROTATION_V1` 向量化复用现有趋势分数，把个股互斥划分为强势加速、趋势延续、中位整理、低位偏弱；市场温度为前两档占比，并提供 5/10/20 日均线、冰点/收缩/扩散/过热区间及证据有效权重
- 市场风格比较强势与低位样本收益中位数，以 ±0.25 个百分点为死区、连续三日才确认；行业周期按强势/低位占比及三日变化形成修复扩散、低位修复、极弱钝化、方向未明、退潮观察和明确退潮六种状态
- 申万 2021 一级行业使用独立严格层级，用户最多关注 30 个二级行业且不改写一级总量；东方财富概念全目录按成分 Jaccard ≥ 0.85 合并别名，题材评分固定为 55% 生命周期 + 45% 宽度，A/B/C/D 只表达数据状态
- 宽基 ETF 只纳入名称或跟踪基准可核查的境内核心/策略宽基，资金按“份额变化 × 当日净值”估算，净值缺失才使用收盘价并逐只标记；首次建立约 25 个交易日并显示累计 MA5/20，随后滚动补最近 6 日；Tushare 三年全市场日线/申万/ETF 与 AKShare 概念采用分层采集，18:30 和 09:05 自动刷新，任何来源失败均保留旧快照并明确降级
- 新增 `/api/v1/market/{temperature,structure}`、`/market/analytics/refresh` 与 `/api/v1/rotation/...` 契约；刷新任务接入统一 `/api/v1/jobs`，具备不可变规格、幂等、租约、事件、取消和安全重试
- 新增无前视、状态家数守恒、行业覆盖门槛、题材去重、ETF 公式、快照哈希、偏好隔离、任务租约、API/CSRF、深链、390px 窄屏和减少动态效果回归测试

### 维护屏障、受限进程与持续质量
- 数据布局迁移接入全局停写屏障：有界停止接单并排空 Web、自动化、研究、数据刷新、回测、Lab、轮动和修复 worker；冻结期已有 SQLite 连接进入只读、新库禁止创建，成功、失败或取消后按逆序恢复组件
- 受限 Python 工件在 Windows 使用 Job Object，在 POSIX 使用 `setrlimit` 与进程组，统一限制内存、CPU、输出、文件、运行时间和子进程数量，超限时清理整个进程树
- 新增 `qm doctor --deep`，检查回环绑定、磁盘、全部 SQLite、行情/研究哈希、修复与发布积压、导入环、领域反向依赖、SQLite 绕过和 API 契约；高危项返回非零状态
- 宽 `Exception` 捕获以逐文件审计基线只减不增，关键运行时和存储边界必须记录或传播；新增严格 JSON、FIFO 成本、有限 Parquet 等性质测试，分支覆盖率实测基线固定为 67%
- CI 扩展为 Windows/Linux/macOS × Python 3.10/3.12、Chromium、Rust fmt/check/clippy、受控 mypy、依赖审计、wheel 新环境、CLI/doctor 和 PyInstaller 冷启动门禁
- 公共健康接口只保留 `/api/v1/health/live`、`/ready` 与 `/api/v1/diagnostics`；聚合 `/api/v1/health` 和所有未版本化路径保持 `404`，深度诊断会把回归视为高危

## v0.12.0（2026-07-31）

### 版本化 API 与统一任务账本
- 全部本地业务接口、前端请求、测试和当前文档切换到 `/api/v1/...`；未版本化 `/api/...` 旧路径直接返回 `404`，不提供重定向、兼容别名或双写路由
- 接口按 `market`、`research`、`lab`、`backtests`、`paper`、`portfolio`、`news`、`automation`、`settings` 与 `data` 领域归整；固定保留 `/api/v1/session`、`/api/v1/health/{live,ready}` 和 `/api/v1/diagnostics`
- 新增 `/api/v1/jobs` 统一任务账本，以一致的领域、状态、进度、阶段、尝试次数、恢复能力和链接 DTO 聚合 Research、Data Refresh、Quant Lab 与 Backtest
- 统一任务接口支持详情、事件、取消和重试；Backtest 重试保留双向事件血缘，Research/Data 续跑复用不可变任务，Lab 重试创建引用原任务的新尝试
- Data Refresh 新增持久任务列表读取；前端架构测试禁止重新引用未版本化 API，服务回归明确验证旧 health、backtests、settings 和 news 路径均为 `404`

## v0.11.1（2026-07-31）

### 严格账本、契约与可重复构建
- 实盘账本统一拒绝 `NaN`、`Infinity`、无效日期、非法方向及非正成交数值；新增成交在 `BEGIN IMMEDIATE` 事务中按日期与原始顺序重放库存，阻止直接超卖和补录历史成交造成的时间前缀超卖
- CSV 批量导入在任何写入前完成规范化和全批库存校验，失败时成交与导入批次一起回滚；读取旧异常账本时明确报告完整性错误，不再把无持仓卖出按零成本计入已实现收益
- 全部 API 请求模型统一拒绝未知字段以及藏在 `Any`、列表或字典中的非有限数值；交易方向、现金流类型、数值范围和文本长度在边界处获得一致校验，非法载荷统一返回 `422`
- FastAPI、显式 JSON 响应和 NDJSON 流统一使用严格序列化；回测数据库与原子结果文件、研究运行清单、规范哈希及 Quant Lab 模型清单会把无法表示的浮点或 Decimal 规范化为 `null`，并以 `allow_nan=False` 阻止非标准 JSON 落盘
- 将问题协议、回测数据质量门禁和监听地址校验移至传输无关模块，删除服务层重复实现；新增架构测试阻止领域模块反向依赖 Web、顶层导入环和绕过统一 SQLite 连接策略
- Rust 研究内核拒绝非有限或非正裁剪阈值，并在滚动相关计算前验证规则矩阵，消除异常输入导致的错误结果或索引越界
- 因子截面排名、去均值、缩尾与标准化统一把正负无穷视为缺失值，避免除零产生的极端值污染截面统计或触发 NumPy/Pandas 运行时警告；Quant Lab 多目标优化固定使用稳定的 NSGA-II 采样器
- 原生扩展的 `Cargo.lock` 纳入版本控制；CI 与桌面发布均使用 `--locked`，CI 额外执行 `cargo check` 和零警告 `cargo clippy`，提升跨平台构建的可重复性

## v0.11.0（2026-07-30）

### 可恢复的数据提交与真实交易日
- BarStore 与 ResearchLake 新增持久写入意图、同目录旧文件备份和启动恢复；进程在 Parquet 替换与 SQLite 目录提交之间退出时，新实例会按内容哈希完成提交或恢复旧文件
- 行情缓存元数据新增 SHA-256、行数、大小和修改时间并拒绝绕过提交协议的可读篡改；研究分区每次读取强制核对内容哈希，目录指向缺失或损坏文件时明确报错而非静默跳过
- 研究运行表先写临时文件并原子替换，最终不可变 manifest 最后落盘作为提交标志；同一运行 ID 不能覆盖不同清单或表内容
- 数据目录迁移在切换前再次复核全部源文件及 SQLite WAL/SHM 侧车，迁移期间发生任何写入都会放弃目标并保持原配置；Web 入口同时拒绝活跃研究、回测、行情和自动化任务，热切换后重新绑定后台执行器
- 生产研究不再用普通工作日伪造交易日：官方日历不可用时只能复用研究湖中已验证的本地交易日，否则阻断计划；中证 800 成分扩展和停牌掩码也统一使用官方日历
- pytest 默认禁用用户环境自动注入的 Xonsh 插件，避免其历史清理线程在 Windows 验收结束时产生与项目无关的访问冲突；项目所需插件仍照常加载
- 新增文件替换后目录提交失败、可读 Parquet 被篡改、迁移中并发写入、无可信日历等故障注入回归测试

## v0.10.2（2026-07-30）

### 持久任务租约与统一 SQLite 运行时
- Research、行情刷新、Quant Lab 与回测任务统一使用 worker 所有者、心跳和过期恢复；第二个 Web、CLI 或独立 worker 进程启动时不会再把仍存活的任务误标为 interrupted
- 所有进度、心跳和完成写入均可校验当前所有者，旧 worker 在租约转移后的迟到结果无法覆盖新 worker；服务关闭只中断本进程拥有的任务，并保留安全续跑位置
- 研究计划哈希排除运行 ID 和创建时间；失败重试不再改写原计划，而是保存任务索引、尝试次数和事件流。行情刷新同样保留不可变原始标的清单及分尝试失败记录
- 除旁路迁移器的只读源与备份目标外，所有生产 SQLite 存储接入统一连接工厂，统一 WAL、`busy_timeout`、外键、同步级别和首次初始化退避
- 服务启动改为只安装离线证券快照，不再自动留下跨生命周期的外部目录刷新线程；外部目录改由本地维护接口显式刷新，测试切换数据根目录时同步清理 SQLite 进程内初始化状态
- 受限因子执行与内置示例校验对 Windows 瞬时 `CreateProcess` 访问拒绝执行有界退避重试，不再把终端安全扫描抖动误报为候选因子失败
- 新增活跃租约不误恢复、租约过期接管、旧所有者写入拒绝、逻辑计划哈希稳定和重试计划不可变等并发回归测试

## v0.10.1（2026-07-30）

### 本机安全边界与分层健康检查
- 所有业务写请求在应用边界统一执行本机客户端、可信 Host、同源和双提交 CSRF 校验，补齐 Quant Lab、选股、因子、账本和自选列表等旧路由的保护遗漏；服务配置和启动器同时拒绝非回环监听地址
- 首页启用逐请求 nonce CSP、禁止脚本属性和嵌套，并补齐 `nosniff`、Referrer Policy、Permissions Policy 等安全响应头；共享 API 客户端会按需获取安全会话并为普通及流式写请求附带令牌
- 新增常量时间 `/api/v1/health/live`、最小依赖 `/ready` 和有界后台缓存 `/diagnostics`，避免存活探针打开多个数据库或触发外部组件诊断
- 安全拒绝、迁移锁和未处理异常继续携带请求编号与脱敏问题信息；新增远程访问、伪造 Host、跨来源写入、CSRF 和无存储存活探针测试
- 新增共享 SQLite 连接策略，串行化首次 WAL 初始化并统一 `busy_timeout`、外键和同步级别；修复并行因子首次打开同一 BarStore 时偶发的 `database is locked`

## v0.10.0（2026-07-30）

### 跨资产研究生产流水线
- 新增不可变 `ResearchSpec` / `ArtifactRef` / `ExecutionPlan` 契约和按交易日分区的 Zstd Parquet 研究湖；原始层使用 float64，派生层使用 float32，每个分区记录 schema hash、SHA-256、输入血缘和规格版本，并与现有按标的 BarStore 按需互通
- 新增 A 股日线/复权因子/每日指标、ETF 基础信息/日线和期货合约/日线/主力映射适配；权限不足与短暂故障分开记录，高级分钟权限不会阻断 2000 积分日线基线，官方日历不可用时优先复用本地真实交易日
- 在原 48 个 Quant Lab 策展起点之上新增 6 个跨资产截面因子、1/3/5/7 日前瞻标签和 QM_STYLE_V1 五风格风险暴露；生产任务同步落盘覆盖率、IC/RankIC、按年表现、衰减、分组收益和换手效率
- `qm data` CLI、`/api/research/data` REST 和设置中心新增目录/能力检查、dry-run 依赖与成本计划、历史/增量生产、持久化任务、取消和续跑；Quant Lab 学习模型预测会发布为版本化模型 Artifact，回测可用 `artifact:<kind>:<asset>:<id>@<version>` 锁定读取
- 新增可选 Rust/PyO3 + Rayon 内核，为截面排名、稳健/加权标准化与滚动均值、波动率、相关性提供确定性加速；CI 在 Windows/macOS/Linux 同时编译并验证与 Python 回退的结果奇偶性

## v0.9.11（2026-07-30）

### 六维个股分析、双端进度与行情缓存修复
- 内置并安全适配 ClawHub `stock-analysis-framework` v1.0.0，按基本面、技术面、消息面、资金面、市场心理和宏观政策六条证据链生成结构化完整报告；缺失数据保留章节并降低覆盖率
- 飞书识别“分析贵州茅台”“六维分析 600519”“贵州茅台怎么样”等说法，以单张卡片原位更新真实阶段进度和最终报告；Web 新增证券建议、NDJSON 流式进度、六维证据、风险及情景分析入口
- 当日收盘前生成的 Tushare 响应在 15:30 后自动失效，手动行情维护改为尾部增量同步；整理时间别名导入顺序并明确缓存测试替身的共享状态，全量 Ruff 检查恢复通过
- 不执行上游读取本地持仓并请求新浪的监控脚本；只分析用户明确询问的单一标的，逐单资金流不可用时明确降级为量价代理

## v0.9.10（2026-07-30）

### 两级目录与进阶量化教材
- 手册从 16 章扩充为 21 章，重组为“市场与数据、数学与统计语言、资产定价与衍生工具、因子与信号研究、组合/回测/模型、生产研究”六部；桌面与移动端均采用“部 → 章”两级目录
- 每部新增先修知识、核心问题和完成标准，章节名称及阅读路径按教材依赖重新编排，使手册既可顺序学习，也可按部查阅
- 补齐概率与随机过程、布朗运动与伊藤引理、线性代数与最优化、PDE、CAPM/APT、ARIMA/GARCH/协整、衍生品、固定收益和金融机器学习主线
- 蒙特卡洛、MVO、CAPM、Black–Scholes 与 Greeks、Black–Litterman、VaR/CVaR 和压力测试升级为核心必学；全书提供 11 个可运行 Python 示例、42 道自测和 10 个本地实验工具
- 进阶内容新增市场冲击与实施缺口、双时态 PIT、鞅/Girsanov 测度变换、SDE 强弱收敛、SDF/GMM、状态空间、准/多层蒙特卡洛、美式期权高低偏差界、波动率曲面、XVA、HJM 多曲线和信用风险
- 研究、组合与生产部分新增因子容量、DSR/PBO/Reality Check、Purged/CPCV、稳健与 CVaR 多期优化、事件驱动订单状态机、Euler 风险分配、EVT/Copula、嵌套时序验证、共形预测、模型治理及生产 SLO

## v0.9.9（2026-07-30）

### 量化金融核心课程补全
- 内置帮助从 16 章扩充为 21 章，新增概率与随机过程、布朗运动与伊藤引理、线性代数与最优化、PDE、资产定价与金融时间序列、数值定价、衍生品及固定收益主线
- 将蒙特卡洛、MVO、CAPM、期权定价和 VaR/CVaR 明确列为核心必学，系统补充二叉树、有限差分、APT/Fama–French、ARIMA/GARCH/协整、Black–Scholes/Greeks、Black–Litterman、久期/凸性与期限结构模型
- MVO、蒙特卡洛和 Black–Scholes 新增可运行 Python 示例；全书现含 11 个示例与 42 道自测，并补充数值收敛、模型风险和交叉验证说明
- 金融机器学习新增 PIT 特征工程，以及前视、标签泄漏、幸存者、选择过拟合和执行偏差的系统防线；组合风控新增 VaR 越界回测、CVaR/ES、历史/假设/反向压力测试

## v0.9.8（2026-07-29）

### 当日行情缓存修复与精准增量同步
- 当日收盘前生成的 Tushare 接口响应在 15:30 后自动失效；手动增量同步强制绕过当期接口缓存，不再因重启或重试持续停留在前一交易日
- 当首选数据源未返回更新日期时继续尝试备用源；仍无新行情时标记为 stale，不再误报 ready
- 设置中心的行情维护从整段全量重拉改为尾部增量同步：已缓存标的仅请求最后 5 个交易日的重叠区间，只有未缓存标的才按选定起点初始化
- 复权校准在至少 80% 重叠日支持稳定比例时允许少量上游修订行，使用稳健中位比例合并并修正异常重叠日，无需因单日修订全量重拉

## v0.9.7（2026-07-28）

### 因子候选锚定弹层与 AI 短名称
- 回测因子候选改为输入框锚定的浏览器顶层 Popover，按可用空间自动显示在输入框上方或下方，不覆盖输入内容；移除常驻说明文字，支持右侧按钮打开、鼠标悬停/点击以及方向键、Enter/Tab 选择
- AI 与 GP 自动候选重名时使用全局连续短编号；旧账本中带哈希后缀的重名候选会迁移为 `AI 候选 21`、`GP 候选 11` 等易读名称，同时保留全部历史版本和 slug 解析能力

## v0.9.6（2026-07-28）

### 因子名称唯一性与回测智能补全
- Quant Lab 因子名称新增 Unicode 归一化唯一约束；旧账本重名候选会保留全部历史版本并自动追加短标识，人工创建重名或使用英文逗号时给出明确校验提示
- 统一因子目录合并所有可执行的 Quant Lab 表达式因子；回测和因子体检可按唯一显示名或 slug 解析，直接复制因子库名称不再被误当作原始表达式
- 回测因子输入改为代码编辑器式候选菜单，支持中文名称/slug 搜索、方向键导航、Enter/Tab 插入、已选项排除，并在每个逗号后的当前片段继续自动补全

## v0.9.5（2026-07-28）

### Quant Lab 阶段进度与批次交互
- 研究任务详情按阶段合并高频进度，同一阶段只保留一行并原位刷新标的、完成数与百分比；原始成交/PIT 约束和 PIT 研究包不再生成数千行时间线
- 最近代码挖掘批次点击后立即显示选中与加载状态，并展示所选批次摘要；尚无候选时也能确认运行状态并打开关联任务

## v0.9.4（2026-07-28）

### 首页主要指数与科技候选预置
- 市场首页重新固定展示主要指数走势图并扩大图形高度；行情源暂时不可用时保留指数区块与重试提示，不再让整组图形消失
- 新建候选加入科技优先的常用指数预置，可一键读取科创50、科创100、科创创业50、半导体材料设备、创业板指、创业板50以及沪深300、中证500、中证1000的当前成分
- 首页主要指数新增科创100和创业板50；深交所自编指数读取成分失败时自动切换备用接口

## v0.9.3（2026-07-28）

### 资讯板块评分与大盘情绪
- 资讯结构化标注新增申万 2021 一级行业板块白名单，每条事件持久化最多 5 个板块标签，并可根据相关股票的本地行业映射自动补全
- 资讯研究新增 -100 至 +100 的大盘情绪和板块独立分数，统一使用情绪、置信度、重要度、来源权重与 3 日半衰期计算，同时展示板块方向和有效事件数
- Web 事件流、飞书卡片与微信提醒同步展示相关板块，旧事件只要已有股票代码也可在读取时获得板块映射

## v0.9.2（2026-07-28）

### Quant Lab 队列并发与按需数据加载
- Quant Lab Worker 默认支持 2 个任务并行执行，可在设置中心配置 1–4 个全局执行槽；任务由 SQLite 原子领取，多个进程不会突破并发上限，存活心跳也不会被其他 Worker 误判为中断
- 基本面因子只加载声明所需字段；EP、BP、股息率和小市值复用同一份本地日频估值缓存，非 ROE 因子不再请求季度财务 API，并优先复用 parquet 与 Tushare 接口缓存
- 并行任务对同一份缺失数据合并为一次 API 请求；旧 parquet 即使缺少元信息也可按覆盖范围离线复用，基本面验证同时新增逐标的进度和安全取消检查

## v0.9.1（2026-07-28）

### AutoMiner 启用提示修复
- 选择或提交默认关闭的 Python AutoMiner 时，发现页会直接说明需要先启用安全开关，并可一键前往 Quant Lab 设置，不再显示为通用接口失败

### 量化算法手册深化
- 内置帮助扩充为 16 章量化研究与 A 股交易手册，新增统计推断、因子合成、机器学习和研究协议，并加入 3 个完整案例与 4 幅研究图解
- 全书新增 8 个可运行 Python 示例和 32 道章节自测，系统讲解 OLS、BH-FDR、区块 Bootstrap、参数平台、Purged WFA、概率校准与 OOD

### 本地算法实验与稳定导航
- 在原有 6 个计算器之外新增 OLS 正交化、BH-FDR、概率校准和双资产风险 4 个浏览器实验台，全部离线计算且不保存输入
- 手册小节采用稳定深链，搜索覆盖正文、代码与自测，并新增代码复制回退、四阶段移动目录和窄屏研究图解

## v0.9.0（2026-07-28）

### 共享多周期滚动优化
- Quant Lab 新增持久化 Optuna Pareto Study：固定 756 日训练、每 20 日重训、7 日 purge 和末尾 252 日密封留出，使用共享编码器联合预测 1 / 3 / 5 / 7 日超额收益、原始收益、上涨/扣费后为正概率和分位区间
- Transformer、TCN、GRU 支持 GPU 混合精度、梯度累积、早停和潜变量 OOD 检测，Ridge 提供可复现 CPU 基线；开发期滚动 OOF 单独拟合 isotonic / Platt 校准，密封标签不参与模型或阈值选择
- Study 使用 SQLite 持久化 Optuna 状态和密封滚动块，支持暂停恢复；候选按扣费信息比率、RankIC 和最大回撤形成 Pareto 前沿，并对完整 trial × horizon 假设族统一执行 BH-FDR
- 通过密封门槛的模型只创建 Shadow Candidate；Study、折叠、校准、工件哈希、预测 OOF 和偏差审计全部进入版本账本，仍需人工批准后才能成为 Champion

### 可信 A 股回测与研究门禁
- production 研究把稳定总收益信号流与未复权成交流分离，使用 PIT 中证800历史成分、官方交易日历、真实公告日基本面、实际涨跌停/停牌和复权因子；任一必要字段或成分日覆盖缺失都会阻断生产研究
- 回测工作台新增 research tier 与 Quant Lab OOF 版本策略，严格按 T 日收盘信号、T+1 未复权开盘成交，模拟公司行动后的总收益单位，并校验 manifest 与预测工件 SHA-256；Sandbox 模型不能用于 production
- 报告新增 Sortino、Profit Factor、Expectancy、VaR/CVaR、区块 bootstrap 置信区间、市场状态、成本压力、容量参与率和持仓周期 MFE/MAE，结果保留模型贡献和降级证据
- Web、REST 和 `qm lab optimize/studies/resume/audit` 提供固定协议、Pareto 证据、折叠时间线、恢复与偏差审计入口；每周自动研究只运行一次最多 10 小时的 production Shadow Study

### 因子鲁棒性晋级门禁
- 统一因子验证新增带固定种子的循环移动区块 Monte Carlo，输出正向 IC 概率、ICIR 与扣费年化收益 95% 区间，保留日序列的自相关结构
- 参数敏感性只扰动安全 DSL 的时序窗口常量，并复用受限 Python AutoMiner 的参数平台邻域；WFA 逐折记录训练/测试 IC、保留率、同号性、离散度和最差折
- 穿透性测试按年度、上涨/下跌趋势、高/常波动、高/低流动性及个股绝对贡献集中度拆解，任何失败项都写入版本账本和候选软门禁
- Quant Lab 因子证据新增四项鲁棒性状态卡，直接展示 Monte Carlo、参数敏感性、WFA 和穿透测试的通过情况与核心诊断

### AI 自动挖掘量化因子
- Quant Lab 新增默认关闭的 Python AutoMiner：最多 3 次模型调用、24 个受限代码候选和 3 个 Pareto 入围候选，支持手动运行与每周一次调度
- 候选只允许白名单 pandas/numpy 向量代码，并在独立子进程执行；导入、I/O、网络、反射、循环和负向 shift 会被拒绝，工件按内容哈希不可变保存
- 本地研究严格执行 TRAIN / VALID / 密封 TEST、purge、参数平台、前视/递归审计、候选族 FDR 与 PIT 数据门禁；TEST 不回流选参，最终仍需人工批准
- 新增 AutoMiner 预览、批次与候选 API、CLI 参数、工作台三段边界和运行账本；生产决策可加载批准后的 Python 工件，异常时保持确定性回退

### 内置量化研究与交易手册
- 页头新增帮助入口，系统讲解 A 股规则、行情数据、统计、因子验证、可信回测、组合风控与项目工作流，并支持全文搜索、章节深链和页面跳转
- 新增复利年化、往返成本与盈亏平衡、风险仓位、回撤恢复、夏普比率和 RankIC 六个本地计算工具，只读复用项目交易参数
- 手册列出交易所与监管官方来源，标注规则核验日期，并明确区分真实市场制度与当前回测执行引擎的近似边界

## v0.8.12（2026-07-28）

### 后台状态条紧凑布局
- 后台状态条移至页面左下角；收起时按内容缩窄，展开时限制为紧凑宽度，减少对工作区内容的遮挡

## v0.8.11（2026-07-28）

### 回测实验历史布局修复
- 实验比较复选框不再继承普通输入框的最小宽度与内边距，每行选择框固定在独立网格列中，不再遮挡实验名称

## v0.8.10（2026-07-28）

### AI 因子发现稳定性与进度反馈
- 每轮模型请求首次等待 180 秒，失败后按 240、360、480 秒窗口自动重试；超时、限流和临时服务异常会记录具体尝试、等待上限与下一步
- 后续轮次用尽重试时保留前序可用候选并标记“部分完成”；执行器心跳与终态阶段持续写入任务账本，失败任务不再看起来卡在 58%

### Quant Lab 研究任务详情
- 全部研究任务均可从概览、队列表格或底部进度条打开右侧详情，查看参数、当前阶段、执行器心跳、事件时间线、结果与警告
- 失败任务展示原因与恢复建议，已结束任务支持按原参数重跑；AI 发现与模型训练等高成本任务会在重跑前再次确认

## v0.8.9（2026-07-28）

### 模拟账户创建提速
- 大型自定义候选改为一次批量校验证券主数据，创建模拟账户不再逐只重复打开数据库和执行模糊搜索
- Hybrid v2 策略固化直接复用已解析的候选快照，同一次创建不再重复加载整份候选

## v0.8.8（2026-07-27）

### 决策行情缩放适配
- 今日入选的内联行情图改为按表格实际可见宽度布局，页面缩放时不再把图形或最右侧列推到可见范围之外
- 展开行情仅调整页面纵向位置，不再改变决策表格的横向滚动位置

## v0.8.7（2026-07-27）

### 缺失行情收益口径与 pandas 兼容性
- 收益率计算不再隐式前向填充缺失价格，停牌或数据缺口会保留为空值，避免伪造零收益并消除批量因子计算的重复预警
- 因子换手分析与策略权重矩阵改用类型稳定的空值；回测、基准指标与盘中变盘检测显式固定收益率缺失值口径

## v0.8.6（2026-07-27）

### GitHub 发布凭据路由加固
- 安装发布 hooks 时自动为 HTTPS origin 绑定目标 GitHub 账号和完整仓库路径，避免系统凭据管理器误用另一个账号
- 自动 push 强制启用 TLS 校验与 `credential.useHttpPath`；固定 GitHub 解析不可用时仍会回退普通连接

## v0.8.5（2026-07-27）

### 版本提交与 GitHub 自动同步
- main 上的版本提交完成后自动重试推送到 origin/main；普通功能分支和 Claude 归档分支不会自动上传
- pre-commit 强制核对版本递增、实际发布日期和 CHANGELOG，并在上一版本尚未同步时阻止继续发布
- 网络或授权失败会保留本地提交与 `.git` 待同步标记，可用统一的 install、status、check 和 push 命令检查及恢复

## v0.8.4（2026-07-27）

### Hybrid v2 决策与 Quant Lab Champion
- 决策页新增“扣费风险收益、短期命中收益、稳定可解释”三种策略画像；规则基线按滚动 ICIR 自适应加权并限制单特征权重，候选同时展示经验分箱校准的上涨概率、扣费后预期、置信度、模型一致性及波动分位风控位
- Quant Lab 的已批准表达式因子和学习模型可按候选、1/3/5/7 日周期、策略画像及精确 / A 股范围设为 Champion；运行时自动选择最匹配版本，规则基线始终保留，组件加载或覆盖失败时按可用权重自动回退
- Ridge 与可选深度模型共享 48 维无未来特征、同日截面缩尾和标准化；训练切分按持有期 purge，正式推理从样本外验证起点开始，并校验 manifest 路径和工件 SHA-256，训练完成后默认只进入影子候选
- 决策 API、CLI、收盘自动化、回测工作台和多账户模拟盘共用 Hybrid v2；回测任务与模拟账户在创建时固化 Champion、权重、画像和策略哈希，历史实验不随后续部署漂移，模拟盘仍不会连接真实券商
- 决策页增加 Champion 健康、有效组件、校准样本与 Brier 摘要，可渐进展开模型版本、权重、警告和单股贡献；保留表内 K 线、自选与关注操作，并完成 390px 窄屏适配

### 资讯标注运行反馈
- 资讯批量标注接入统一 NDJSON 流式请求和后台状态；部分失败会保留已完成结果、给出可恢复提示并进入退避重试

## v0.8.3（2026-07-27）

### 回测与模拟成交一致性加固
- 历史回测不再静默丢弃涨跌停或缺价订单；未被新信号替代时会跨交易日重试，并在结果中保留受阻原因
- 回测产物新增稀疏持仓历史与受阻订单明细，页面可直接核对成交、限制和重试路径
- 模拟盘买入目标与回测统一按完整目标金额预留费用，同一行情和策略快照得到一致的开盘成交
- 未关联 Quant Lab 批准版本的策略仍可创建模拟账户，但会持续显示来源风险；自动账户纳入每日处理
- 已移出候选的持仓继续报价和卖出，但不再参与策略排名或被重新买入

## v0.8.2（2026-07-27）

### 决策页内联行情与快捷关注
- 今日入选股票可在决策表格中直接展开日线与分钟 K 线，不再跳转市场页打断分析
- 行情详情新增自选与关注快捷按钮，当前状态即时可见，并支持就地加入或移出
- 单项展开、键盘操作、加载与失败反馈及窄屏布局均已补齐，快速切换标的不会显示过期行情

## v0.8.1（2026-07-27）

### 后台状态与研究数据门禁
- 底部后台状态统一汇总数据源熔断、数据刷新、资讯、自动化、Quant Lab 与回测问题，活动问题恢复后自动消失
- 普通请求和流式任务使用统一问题协议，错误包含严重度、阻断状态、恢复建议与请求编号，业务警告也会进入后台状态
- 回测缺少关键价格、标的、交易日、信号或成交时停止；部分数据只有经用户确认后才会重新入队计算
- 回测复现清单新增实际区间、可用标的、可成交信号和基准状态

## v0.8.0（2026-07-27）

### 可复现回测与多账户模拟盘
- 回测升级为可取消、可恢复的持久化任务，保存策略配置、数据快照、成交假设和完整结果，并支持历史比较与 JSON/CSV 导出
- 中证800回测按历史成分过滤，固定候选明确提示幸存者偏差；已完成实验可带不可变策略快照创建模拟账户
- 模拟盘支持隔离的命名账户、手动或显式自动确认、提案与订单周期，并严格在信号后下一交易日开盘撮合
- 回测和模拟盘共用涨跌停、费用、滑点与整手规则；受阻订单跨日保留，旧模拟账本无损迁移为暂停的历史账户
- Web、CLI 与 Bot 不再按最新收盘价直接写成交，统一经过提案、确认和待开盘流程

## v0.7.0（2026-07-27）

### 跨市场证券主数据与智能代码解析
- 随安装包发布 33,984 条压缩证券快照，覆盖内地股票、场内基金、沪深/中证指数、港股及 Nasdaq Trader 美国上市目录；首次启动事务化导入本地 `security_master.sqlite`，断网时名称、市场和品种仍可查询
- 新增统一证券主数据与别名索引，支持 `600519`、`sh600519`、`600519.ss`、`SSE:600519`、`700.hk`、`NASDAQ:AAPL`、中英文名称、全拼和拼音首字母；内部统一为 `.SH/.SZ/.BJ/.CSI/.HK/.US/.JP/.KR` 代码
- 短代码跨市场重名时返回候选而非静默猜测，例如 `700` 同时展示 `000700.SZ` 和 `00700.HK`；单只添加与批量编辑均可逐项确认，选择历史只参与排序、不绕过确认
- 候选工作台加入 300ms 防抖、完整键盘操作和不受容器裁切的搜索列表，结果展示名称、代码、市场、交易所及股票 / ETF / 指数类型；未知标的和无可验证日线路由的标的不能保存
- 接通 AKShare ETF 历史、Tushare `fund_daily` 与 `.CSI` `index_daily` 路由，`589160.SH`、`931743.CSI`、`950125.CSI` 等标的可正常加载；旧候选中误写为 `.SH` 的同码中证主题指数自动校正
- Tushare、Nasdaq 官方目录与 Yahoo 按需查询使用独立健康状态，在线刷新只做 upsert 并保留旧名称/别名；设置数据源诊断新增主数据总量、分市场/品种覆盖和来源状态
- 打包配置包含压缩快照，新增离线、解析歧义、旧名称迁移、ETF/CSI 路由、HTTP API 和资源打包回归测试

## v0.6.1（2026-07-27）

### 我的股票行情板块
- 市场首页新增独立的“我的股票”板块，自选、关注与实盘持有可像指数和商品一样直接查看走势图
- 同一股票跨列表自动去重，并显示自选、关注、持有来源标签；点击卡片可继续查看日线与分钟 K 线
- 添加或移除标的后自动刷新行情分区；本地缓存优先展示，数据源暂不可用时仍保留已有图表

## v0.6.0（2026-07-27）

### 行情增量缓存与数据源调度
- 市场页先显示本地缓存，完整历史覆盖不再因 TTL 反复联网；近期行情只补缺失边界和 5 个交易日复权校准窗口
- 东方财富、新浪、中证、Yahoo 与 Tushare 按真实上游并行调度并合并相同请求；全球参考市场改为 Yahoo 批量下载
- 数据源失败状态和 5 / 15 / 30 分钟熔断跨重启保留，代理或限流故障后直接降级并汇总日志，减少 AKShare 与 yfinance 错误刷屏
- 设置中心新增全量刷新预览、确认、持久化进度、取消、重启续跑和失败项重试；校验失败时原缓存保持不变
- 数据源检测并行探测真实行情端点，展示熔断和脱敏代理信息；面板加载保持输入标的顺序

## v0.5.2（2026-07-27）

### 推送订阅与资讯速览
- 每个飞书群、管理员私聊或微信目标可分别订阅重要资讯、盘中变盘、收盘状态、任务结果和任务失败；五类全部取消后自动化调度与 Bot 监听仍会继续运行
- 内容订阅成为绝对门控，高分资讯与严重任务失败也不会绕过用户已关闭的类别；手动测试推送仍可显式验证消息通道
- 单条资讯和定时摘要直接展示摘要、利好 / 利空 / 中性判断、情绪值、重要度、相关标的及核查依据，资讯任务不再重复发送泛化的完成通知
- 自动化推送设置与资讯来源保存增加就地保存中、成功和失败反馈；操作审计默认折叠并在首次展开时读取
- 恢复设置列表字段解析，修复设置中心首次加载后可能因脚本异常而被禁用的问题

## v0.5.1（2026-07-27）

### 终端日志与异常定位
- 启动、后台任务与所有 `qm` 命令统一使用紧凑终端格式，正常状态与错误根因不再被 Uvicorn 访问日志、调度器信息和第三方 SDK 输出淹没
- 首次异常在终端保留最多 4 个 QuantMaster 业务帧，同类错误 10 分钟内只显示摘要；每次完整 traceback 均写入约 50 MB 的轮转日志
- 数据源重试与成功降级不再逐次展开异常栈；Lark 日志统一进入 stderr，命令 JSON/表格继续独占 stdout
- `--verbose` 可放在任意子命令位置，`QM_LOG_LEVEL=DEBUG` 可用于脚本；非 TTY 自动使用纯文本，日志与异常信息会遮蔽常见凭据

## v0.5.0（2026-07-27）

### 候选工作台与导航重构
- 新增顶层“候选”工作台：桌面端双栏浏览，窄屏使用选择器；支持查看证券名称与代码、搜索、分页、逐只添加、批量编辑和明确保存草稿
- 自定义候选支持新建、复制、从自选/关注/持有导入、重命名与删除；重命名同步自动化及 Quant Lab 引用，删除在用候选前必须选择合法替代项
- `demo` 保持固定只读，`csi800` 按目标日期分别读取沪深300与中证500最近可知快照，不把动态成分误存为固定列表
- 决策、Quant Lab、因子验证、回测、模拟盘、自动化与设置统一接入候选目录，并可从选择器直接查看详情
- 顶栏按“市场、资讯｜候选、决策｜Quant Lab、回测｜模拟盘、实盘｜自动化”分组重排，设置保持为最右侧独立入口
- 增加候选 API、引用安全、PIT 成分、桌面浏览器交互与 390px 无溢出的回归覆盖

## v0.4.17（2026-07-27）

### 实验室色彩校准
- Quant Lab 的画布、面板、分隔线与文字层级改为冷调中性色，减少偏棕色感
- 保留蓝色主操作、琥珀色提示与状态色语义，不改变现有布局和交互

## v0.4.16（2026-07-27）

### 飞书群聊话题记忆
- 普通群消息只进入本地上下文，不触发命令或回复；只有真实 `@QuantMaster` 才进入回复路由
- 上下文按当前问题、股票代码、主题词、被回复消息和最近轮次组合，不再使用固定一小时窗口
- 原文超过预算后由已配置 LLM 压缩为结构化话题记忆，保留标的、观点、分歧、结论、时间线和待确认项；压缩成功后才清理已覆盖原文
- 无法匹配固定白名单命令的 `@` 问题由 LLM 结合话题记忆回答，但不能执行交易、设置、任务或账本操作
- 需要飞书敏感权限 `im:message.group_msg` 才能接收普通群消息；只开 `group_at_msg` 时仍可回复 @，但没有群聊上下文
- 所有用户可见的权限称谓统一为“管理员”

## v0.4.15（2026-07-27）

### 设置热应用与飞书诊断
- 设置中心补齐资讯批量标注、消息面因子和 Quant Lab 的研究范围、周期、调度、预算、设备及云端样本开关
- 普通字段停顿片刻或离开字段后自动校验、保存并热应用；API Key、Token 与 App Secret 等待输入完成再提交，避免半截凭据
- 自动化调度器、单个 Bot 通道和 Quant Lab Worker 支持安全重配，关闭 Worker 不会中断当前研究；只有服务 host/port 仍需重启
- 飞书凭据维护收口到设置中心，增加凭据、自动化运行时、WebSocket 长连接、入站消息事件和会话绑定五阶段诊断及移除入口
- Quant Lab 工作台跟随统一候选、研究起点与验证周期；数据根目录切换后自动重绑运行时，独立 Worker 也会监听配置更新

## v0.4.14（2026-07-27）

### 资讯标注流式进度
- 资讯工作台在标注期间显示真实批次、完成比例、成功/失败数和已用时间，不再只显示一个长期等待状态
- 每批 5 条标注写入数据库后立即刷新对应事件卡片、覆盖率统计和消息面因子，后续批次仍在运行时即可查看首批结果
- 流式接口使用 NDJSON 逐批推送已持久化结果；单批失败继续沿用分级退避与重试队列，并在进度区明确反馈
- 增加逐批持久化与流式 API 回归测试，Chromium 验证 5/6 条中间态实时可见且 390px 窄屏无横向溢出

## v0.4.13（2026-07-27）

### Bot 内置使用说明
- 飞书与微信 Bot 支持“帮助 / 使用说明 / 怎么用 / 你会什么”等入口，未绑定会话也能获得明确绑定指引
- 绑定成功后主动给出第一条自然语言示例；无法识别时说明没有执行操作，并引导查看完整帮助
- 使用说明按私聊和群聊展示不同权限，群聊明确提示需要真正 `@QuantMaster`
- 新增“大盘怎么样、查看候选股、提醒少一点、提醒敏感一点”等自然语言说法

## v0.4.12（2026-07-27）

### 服务器安全退出
- Windows 命令行窗口关闭、注销或关机时，请求 Uvicorn 执行正常停机并等待 FastAPI 生命周期清理
- 后台监视 `qm.exe` 启动器进程；启动器意外消失后自动停止服务，避免遗留 Python 进程继续占用 8686
- `Ctrl+C`、正常返回和父进程退出统一回收自动任务、Bot 长连接、调度器与 Quant Lab Worker
- `qm app` 的延迟浏览器任务改为守护线程，服务提前退出时不会反过来阻塞进程结束

## v0.4.11（2026-07-27）

### 后台消息整理
- 流式任务的阶段消息改为原位更新，一个任务只占一条主记录；同一接口的重复结果合并为最新状态
- 问题记录先显示明确处理建议，后端原因、接口路径与请求编号收进可展开的“诊断信息”
- 筛选简化为“全部 / 问题 / 进行中 / 已完成”，后台错误不再强制弹开消息面板
- 摘要栏始终优先显示尚未解决的问题，成功重试后自动移除对应旧错误

## v0.4.10（2026-07-27）

### 飞书群绑定修复
- 修复 `lark-oapi 1.7.1` 已在策略层放行群 @消息、但未回写 `mentioned_bot`，导致适配器重复检查后误丢消息的问题
- 同时清理 SDK 标准化后的 `@QuantMaster` 显示名，使群绑定命令能够匹配并执行
- 增加真实 SDK 形态的群消息回归用例，覆盖“事件已收到但绑定一直 pending”的场景

## v0.4.9（2026-07-27）

### 页面位置记忆
- 切换一级页面后刷新浏览器，继续停留在刷新前的页面，不再固定跳回市场页
- 页面位置按浏览器标签页隔离，避免多个 QuantMaster 页面互相覆盖导航状态
- 已失效或不可访问的页面记录会自动清理，并安全回退到市场页

## v0.4.8（2026-07-27）

### 飞书绑定向导
- 绑定流程改为“验证消息事件 → 绑定管理员私聊 → 由管理员绑定群聊 → 测试推送”的明确顺序
- 生成绑定命令后每 2 秒检查结果，页面实时显示等待、事件已收到、成功、过期或订阅异常
- 群绑定明确要求在飞书中真正 `@QuantMaster`；未绑定管理员时不能提前生成群绑定码
- 绑定码在错误会话中不会被消耗，避免私聊码误发到群后必须重新生成
- 绑定成功后自动刷新目标并开放测试按钮；未绑定测试会给出原因而不是静默禁用
- 入站消息按私聊/群聊分别统计，群事件缺失时会针对 `im:message.group_at_msg:readonly` 给出排查提示

## v0.4.7（2026-07-27）

### 决策快照布局
- 历史决策快照的“前三候选”改为每只股票独占一行，名称和代码分层显示
- 日期、周期、仓位分别使用 96px、72px、72px 稳定列宽，周期内容禁止拆字
- 快照表设置合理最小宽度，窄屏通过局部横向滚动保留完整信息

## v0.4.6（2026-07-27）

### 飞书接入诊断
- 自动化页面显示飞书入站消息数量和最近接收时间，明确区分“WebSocket 已连接”与“消息事件已投递”
- 长连接正常但没有收到事件时，直接提示核对长连接订阅方式、`im.message.receive_v1`、单聊权限和应用发布状态
- 未绑定目标的测试按钮改为“测试（先绑定）”，点击后给出绑定指引，不再静默禁用

## v0.4.5（2026-07-27）

### 资讯管线加固
- 资讯列表正文限制为 2000 字符，展开长篇资讯时再按需读取完整正文，避免自定义来源
  让首屏响应和浏览器内存无界增长
- LLM 标注只接受规范 A 股代码、固定事件/范围/紧急度枚举和有限数值，异常输出不会
  污染归档或 `news_sentiment` 因子
- 禁止将 Token、API Key 等凭据写入来源 URL，普通请求头也增加数量、名称和长度限制
- “处理待标注”只消费待处理与可重试队列，不再重置并重复标注全部历史资讯
- 精确正文哈希用于跨来源因子去重，并增加敏感 URL、长正文和不可信标注回归测试

## v0.4.4（2026-07-27）

### 资讯研究工作台
- 资讯页升级为可筛选事件流，加入证据展开、标注覆盖率、30 日消息面走势、
  个股聚焦与来源运行健康度；来源可在设置中心集中管理
- 支持内置来源启停与调权，以及 RSS、JSON 点号路径、HTML CSS 选择器三种声明式来源；
  Token 进入系统凭据库，抓取包含公网校验、跨域凭据剥离和 5MB 响应上限
- 新资讯先去重归档再批量标注，失败分级退避；结构化资讯长期保留，原始响应默认缓存 7 天
- 快讯、官方、定期三组默认按 10、15、60 分钟调度；修复 60 分钟 Cron 步长导致的启动失败
- `news_sentiment` 接入统一因子入口，按首次获取时点、置信度、重要度、来源权重和
  3 日半衰期计算，15:00 后消息顺延到下一交易日
- 新增 10 项来源、解析器、缓存、CSRF、调度和因子对齐测试，并通过桌面及 390px Chromium 验收

## v0.4.3（2026-07-27）

### 决策图表修复
- 修复决策数据分阶段渲染时反复替换图表 DOM，缓存的 ECharts 实例仍绑定旧节点，
  最终导致“牛熊分与市场宽度”图形空白的问题
- 图表工厂现在会识别已销毁或已脱离当前容器的实例，并在可见节点上自动重建
- 增加连续渲染回归测试，确保最终容器包含有效画布

## v0.4.2（2026-07-27）

### 自动化修复
- 修复快速重启后旧调度租约尚未过期，导致新进程永久停在 `standby`、飞书 Bot 只显示“已配置”的问题
- 备用进程每 5 秒重试租约；旧实例退出或租约过期后，自动接管定时任务与飞书长连接
- 增加调度接管回归测试，覆盖首次抢占失败、随后自动恢复的场景

## v0.4.1（2026-07-27）

### AI Quant Lab
- 统一高密度研究工作台：总览、AI 发现、模型实验、因子库与可恢复任务队列
- 因子目录扩充到 48 项，加入不可变版本、数据快照、统一验证、AI 修正、人工审批与 Champion 账本
- 支持 Ridge 轻量基线，以及可选 MLP、TCN、GRU、Transformer、去噪自编码器
- 中证800历史成分按自然月读取并独立向前填充，固定候选明确标为 sandbox
- Web 内置 Worker 与 `qm lab worker` 共用跨进程幂等调度、夜间窗口和每日计算预算

### 市场图表修正
- 市场概览小图改为相对起点的涨跌幅，统一显示 0% 虚线基线
- K 线大图与上方市场小图之间增加 32px 分区间距，并预留页头滚动偏移

## v0.4.0（2026-07-27）

### 新增
- **本地因子实验室**：统一管理数据集、实验、验证报告和模型产物，提供异步
  worker、API 与 CLI 工作流
- **可配置资讯数据管线**：支持内置、RSS、JSON、HTML 来源，保存原始响应缓存、
  内容指纹、解析状态和可恢复分析队列
- **消息面因子**：按首次入库时间、置信度和时间衰减构造质量加权因子，盘后消息
  自动进入下一交易日，避免回测信息泄漏

### 优化
- 实验验证补充 walk-forward、FDR 校正和可追溯运行记录
- 资讯分析失败使用分级退避重试，重复内容不会反复消耗 LLM 和数据源额度

## v0.3.2（2026-07-27）

### 界面修正
- 提高页头版本号与发布日期的字号和对比度，版本信息首屏可见
- 设置入口精简为纯图标按钮，齿轮按八方向旋转对称重新绘制并严格居中
- 首页直接注入版本摘要；更新日志接口异常时不再隐藏版本入口

## v0.3.1（2026-07-27）

### 新增
- **飞书 Bot 主通道**：企业自建应用通过官方 SDK 长连接接收私聊和群聊命令，
  重要消息、变盘与定时任务结果使用可核查的结构化卡片推送
- **分目标推送强度**：保守、均衡、敏感三档可在自动化页面选择，也可在绑定会话中直接修改；
  支持管理员私聊与群聊独立配置
- **可靠自动化**：定时扫描、事件冷却与次数上限、可靠发件箱、失败重试、去重和审计记录

### 优化
- 腾讯微信 ClawBot 定位为轻量文本提醒，完整命令、卡片和重要消息交互以飞书为主
- 飞书长连接支持受控启动和停止，服务退出时主动释放连接

### 测试
- 覆盖飞书连接生命周期、标准化消息、结构化卡片、推送策略、绑定、发件箱与接口安全

## v0.3.0（2026-07-27）

### 新增
- **市场决策闭环**：日线/分钟线统一入口，多窗口牛熊状态、板块强弱、未来
  1/3/5/7 日概率与每日短周期候选集中到决策工作台
- **数据韧性**：AKShare 重试与缓存补全、Tushare 2000 积分接口及跨重启缓存；
  渐进式加载会即时呈现已完成的行情与决策结果
- **本地管理能力**：我的标的、设置中心、候选、券商 CSV 导入、配置快照、
  自动任务以及微信/飞书消息通道
- **桌面运行**：`qm app` 与 PyInstaller 三平台单文件构建流程

### 优化
- 设置入口从业务功能导航移至右上角，页头主导航只保留业务模块
- 页头新增低干扰版本号与发布日期；点击后可直接查看版本更新日志
- 每日候选补全股票名称，行情与决策加载失败时保留已经展示的数据
- 窄屏重排不隐藏关键功能，动效遵循 `prefers-reduced-motion`

### 测试
- 覆盖行情韧性、分钟线、市场决策、设置安全、自动任务、CSV 导入与前端管理流程

## v0.2.0（2026-07-26）

### 新增
- **基本面/价值因子**：`data/fundamentals.py`（每日 PE/PB/股息率/市值 + 季度 ROE，
  报告期 +45 天发布滞后对齐防未来函数，Parquet 缓存）；`factors/fundamental.py`
  产出 ep / bp / dividend_yield / small_cap / roe 五个因子
- **样本外验证**：`backtest/validation.py` — train/test IC 对比（含衰减度与
  稳健/衰减/疑似过拟合/失效判定）、滚动分段 walk-forward IC、参数网格扫描
- **回测报告**：`backtest/report.py` — 年度收益表、月度收益表、成交统计，
  JSON 可序列化的 full_report
- **回测止损/止盈**：开盘价触线清仓，跌停顺延、当日不回补，成交记录标注原因
- **实盘净值曲线**：`portfolio/nav.py` — 从账本逐日重建资产/现金/浮盈，
  TWR 时间加权净值（出入金不扰动收益），与基准对比
- **多因子工具**：`factors/composite.py` — 因子相关性矩阵、IC/ICIR 动态加权合成
  （权重 shift 防未来函数、负 IC 自动反向）、截面正交化、贪心去冗余选择
- **CLI**：`qm validate` / `qm grid` / `qm fund-test` / `qm ledger nav`、
  `qm backtest --full --stop-loss --take-profit`
- **仪表盘**：因子页样本外验证区块；回测页止损止盈输入与年度/月度收益表；
  实盘页 TWR 净值曲线对比基准；挖掘结果点击表达式一键送检

### 修复
- `.gitignore` 的 `data/` 模式误忽略 `quantmaster/data` 源码包（v0.1 远端仓库
  缺失整个数据层），改为 `/data/`
- 爬虫入库去重计数使用 `cursor.rowcount`
- ECharts 本地化打包，仪表盘不再依赖 CDN（离线/大陆网络可用）

### 测试
- 65 → 135 项离线测试

## v0.1.0（2026-07-26）

- 多市场数据层（AKShare / yfinance / Tushare，自动降级 + Parquet 缓存）
- 因子表达式引擎（AST 白名单）、12 个内置量价因子、IC/分层分析
- 遗传规划 + LLM 因子挖掘；统一 LLM 客户端（Anthropic/OpenAI/兼容协议）
- AI 财经快讯爬虫与舆情因子
- A 股规则回测引擎（T+1/涨跌停/费用/整手）、模拟盘
- 实盘账本（FIFO/TWR/XIRR）
- FastAPI + ECharts 仪表盘、CLI、CI
